# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# storybook
storybook-static

*storybook.log

# storybook examples
/src/stories

.claude/settings.local.json
# Vendored impeccable skill: tracked (committed) but excluded from Biome via
# useIgnoreFile. On skill update: `git add -f .claude/skills`.
.claude/skills/
.vscode
plans
docs/brainstorms
.harness-backup/
# Harness durable layer
harness.db
harness.db-wal
harness.db-shm
scripts/bin/harness-cli
scripts/bin/harness-cli.exe
.claude/scheduled_tasks.lock
# Design handoff preview entrypoint — kept on disk for local preview, not committed (decision 0021)
design_src/EduPortal.html
# Agent runtime artifacts — local only
.agents/
skills-lock.json
# Impeccable design-skill local context file — generated by /impeccable, not a canonical Harness artifact
PRODUCT.md
