/** Wire shape for a directory contact (camelCase). */
export type ContactResponseDto = {
  id: string;
  name: string;
  role: string;
  avatarInitials: string;
  color: string;
  isOnline: boolean;
};
