# Test Matrix

This file maps product behavior to proof.

No product behavior has been defined or implemented yet. Do not mark a row
implemented until tests or validation evidence exist.

## Status Values

| Status | Meaning |
| --- | --- |
| planned | Accepted as intended behavior, not implemented |
| in_progress | Actively being built |
| implemented | Implemented and proof exists |
| changed | Contract changed after earlier implementation |
| retired | No longer part of the product contract |

## Matrix

| Story | Contract | Unit | Integration | E2E | Platform | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TBD | Add rows when story packets are created | no | no | no | no | planned | none |
| US-E12.1 | Admin role enabler: nav-config admin 7 items + DEFAULT_ROUTE + UserRole "admin" | yes | yes | no | yes | implemented | validate-grade-range.use-case.test.ts (13), get-setup-progress.use-case.test.ts (4), nav-config.test.ts admin block; mock repo; bun build clean |
| US-E18.1 | Calendar wiring: CalendarRepository remapped from guessed mock-first shape (US-E06.5) to real core `AcademicCalendar` contract — flat wire DTOs (`academicYearId`/`termId`/`status` enum) reassembled to the nested `AcademicYear.terms` domain shape via per-year `listTerms` fan-out; ARCHIVED years filtered from `listYears`; `hasGrades` always `false` from wire (real guard is `409 CALENDAR_TERM_IN_USE`); `createYear` orchestrates create+activate with no rollback on `active-year-exists` conflict; full 14-code `CALENDAR_*` error matrix (fixed `CALENDAR_FORBIDDEN` mismap, added `forbidden`/`invalid-label`/`invalid-term-name`); proactive `ensureFreshSession()` wired into `calendar.di.ts` real branch (playbook step 6). Zero UI/ViewModel change. | yes | yes | no | yes | implemented | `calendar.mapper.test.ts` (6, new), `calendar.repository.test.ts` (29, rewritten — full error matrix + fan-out + createYear orchestration); 243 files / 1306 tests pass (zero regression); tsc --noEmit clean; `bun run build` green; tech-lead review APPROVED |
| US-E18.2 | Staffing wiring: StaffingRepository/DTOs/mapper remapped from guessed mock-first shape (US-E06.8) to real core `Staffing` contract — `departmentId`/`positionTitleId`/`positionAssignmentId` id renames, department `conceptLabel` split into `conceptLabelSuggested`+`conceptLabelCustom`, 6-value `Permission` enum (replaces wrong 4-value), assignment `createdAt`→`assignedAt` + wire `ARCHIVED`→domain `REVOKED`; `activeAssignmentCount` derived via real fully-paginated `position-assignments?status=ACTIVE` count fan-out (by scopeEntityId + positionTitleId); `positionTitleName` joined from titles list, `memberName` falls back to raw `memberId` (no IAM name source — cross-repo gap); `scopeEntityType` derived from the selected title's `scopeType` on create (no new UI field); full ~28-code `Staffing` error matrix (added `archived`/`invalid-scope-type`/`validation`); proactive `ensureFreshSession()` wired into `staffing.di.ts` real branch (playbook step 6). Zero screen-layout change (permission option list + i18n keys swapped). Tech-lead review round 1 found `raw: true` nested inside `params` (5 call sites) instead of top-level axios config — broke real-mode list calls (interceptor unwraps envelope before `parseEnvelope` runs, silently degrading to `network-error`); fixed + covered by a new real-interceptor regression test. | yes | yes | no | yes | implemented | `staffing.mapper.test.ts` (9, new), `staffing.repository.test.ts` (rewritten/extended — full error matrix + count/name derivation + memberName fallback + scopeEntityType derivation + pagination loop + real-interceptor raw-flag regression guard); 62 staffing tests pass; 244 files / 1336 tests pass (zero regression); tsc --noEmit clean; `bun run build` green; gateway smoke not run (BE stack not reachable — HTTP 000 at `:8000`); tech-lead review round 1 Revision Required (raw-flag bug) → fixed, independently re-verified by fe-lead |
| US-E18.6 | IAM member + tenant wiring: paths already MATCH 100% (confirmed against `edu-api/services/iam/docs/openapi.yaml`) — real work is the error-code taxonomy. `IamMemberRepository.mapIamFailure` switched on 7 guessed UPPER_SNAKE codes that NEVER match any real IAM response (ground-truthed against Go source, not just `ERROR_CODES.md`: real wire `error.code` is always the lowercase snake_case i18n key, e.g. `member_already_exists`) — every real error silently fell through to `unknown`. Rewired to the real taxonomy: `forbidden_action`→`forbidden`, `member_not_found`→`not-found`, `member_already_exists`→`member-exists` (renamed from `email-exists`, no consumer relied on the old name), `member_last_admin`→`last-admin`, `invitation_invalid`→`invitation-invalid` (renamed from `invitation-not-found`, real status is 410 not 404), plus 3 new failure types previously unmapped: `member_tenant_inactive`→`tenant-inactive`, `member_invalid_transition`→`invalid-transition`, `invitation_expired`→`invitation-expired`, `invitation_email_mismatch`→`invitation-email-mismatch` (F8). Trimmed speculative `tenantName`/`email`/`name` fields off `iam-member-response.dto.ts` (not on the real wire). `ensureFreshSession()` wired into both `iam-member.di.ts` and `tenant.di.ts` real branches (playbook step 6). Duplication note logged (not fixed): `IamMemberRepository.listMyTenants/switchTenant` duplicate `TenantRepository`'s already-correct implementation and are currently dead code (no Server Action calls `makeInviteMemberAction`). Zero UI/ViewModel change — `IamMemberFailure` has no presentation consumer today. | yes | yes | n/a | yes | implemented | `iam-member.mapper.test.ts` (extended — trimmed DTO shape); `iam-member.repository.test.ts` (rewritten — full corrected error matrix, real lowercase codes); N files / N tests pass (zero regression vs baseline 287/1714); `bunx tsc --noEmit` clean; `bun run build` green; tech-lead review verdict pending |
| US-E18.3 | Subject catalogue wiring: SubjectCatalogueRepository/DTOs/mapper remapped from guessed mock-first shape (US-E06.6) to real core `SubjectParent`/`Subject` contract — id renames (`subjectParentId`/`subjectId`), concept label split (`conceptLabelSuggested`+`conceptLabelCustom`), nested `master` fields + `ResourceRef` round-trip for `exerciseBankRef`/`examBankRef`; `childCount`/`activeChildCount` derived via fan-out subjects fetch (not on wire); `listSubjects(parentId)` fan-out + client-side filter (no `subjectParentId` query filter on `GET /subjects`); `inUse` defaults `false` in real mode (no cheap BE derivation, real 409 still blocks archive); `getSubject.classOfferings` real mode returns `[]` (no reverse subject→classes BE endpoint — cross-repo ask); `restoreParent` is a WEB-ONLY optimistic no-op success (BE has no restore/un-archive endpoint; delegating to the mock repo was rejected — its `restoreParent` returns `not-found` for real non-fixture UUIDs; returns `ok(undefined)` so the screen's optimistic flip works, no server persistence); error matrix fix (`SUBJECT_INVALID_CODE`, new `subject-archived` for `SUBJECT_ARCHIVED`/`CLASS_SUBJECT_ARCHIVED`); proactive `ensureFreshSession()` wired into `subject-catalogue.di.ts` real branch. Zero UI/ViewModel change. | yes | yes | no | yes | implemented | `subject-catalogue.mapper.test.ts` (12, new — id renames, concept split, master nesting/defaults, ResourceRef round-trip, request-body shaping); `subject-catalogue.repository.test.ts` (32, rewritten — full ~20-code error matrix + fan-out pagination/count derivation + listSubjects client-side filter + getSubject empty classOfferings + restoreParent WEB-ONLY fallback + request-body shape assertions + real-interceptor raw-flag regression guard); 286 files / 1680 tests pass (zero regression, baseline 285/1650); `bunx tsc --noEmit` clean; `bun run build` green; `bun lint` clean |
| US-E18.5 | Admin roster wiring: RosterRepository/DTOs/mapper remap from guessed mock-first shape (US-E06.7) to real core `Classes`/`Enrollment` contract — `classId`/`academicYearLabel` renames on `ClassSummary`'s class-list source; homeroom display name derived via single `GET .../homeroom-teacher` fan-out (404-as-null). **Major finding**: `EnrollmentResponse` (roster listing) carries no name/dob/gender/status at all, and no IAM endpoint can resolve `studentMemberId` → profile for another user — `getClassRoster`/`getSearchPool` stay mock-first permanently (hybrid DI composite, same pattern as US-E18.4's `listTeachers`); cross-repo ask #9 logged. Error matrix corrected: guessed `STUDENT_NOT_FOUND`/`CLASS_ACCESS_FORBIDDEN` removed (don't exist on the real API), `CLASS_FORBIDDEN` added. No new `RosterFailure` types, no i18n changes. Zero UI/ViewModel change. `ensureFreshSession()` wired into `admin-roster.di.ts` real branch (playbook step 6, first time in this DI factory). | yes | yes | n/a | yes | implemented | `roster.mapper.test.ts` (rewritten — `toClassSummary` id/field renames + injected homeroom enrichment); `roster.repository.test.ts` (rewritten — `getClasses` full error matrix incl. new `CLASS_FORBIDDEN`, multi-class homeroom fan-out incl. 404-as-null, `getClassRoster`/`getSearchPool` stub tests asserting no HTTP call, enroll/unenroll/transfer kept, raw-flag real-interceptor regression guard adapted to `classId`/`academicYearLabel`); 287 files / 1714 tests pass (zero regression, baseline 287/1712); `bunx tsc --noEmit` clean; `bun run build` green; tech-lead review APPROVED (independently re-verified `ClassResponse`/`EnrollmentResponse`/error-code claims against `edu-api/services/core/docs/` — no blocking findings, one non-blocking CONSIDER: add a test for a non-404 homeroom-fetch error propagating through `getClasses`) |
| US-E18.4 | Class management wiring: ClassManagementRepository/DTOs/mapper remapped from guessed mock-first shape (US-E06.3) to real core `Classes` contract — `classId`/`academicYearLabel` renames; `studentCount`/`homeroomTeacherId`/`homeroomTeacherName` NOT on the wire, derived per-row via roster-count (`GET .../students`, paginated to completion) + homeroom-assignment (`GET .../homeroom-teacher`, `404 CLASS_ASSIGNMENT_NOT_FOUND`→`null`) fan-outs scoped to the current list page only; `renameClass` backfills a missing `name`/`gradeLevel` via `GET` before `PATCH` (real `UpdateClassRequest` requires both) then re-runs both enrichments; `assignHomeroomTeacher` request field renamed to `teacherMemberId`; full ~15-code `Class`/`TeachingAssignment` error matrix (5 new failures: `class-archived`, `homeroom-teacher-not-found`, `assignee-not-teacher`, `invalid-name`, `invalid-academic-year`); proactive `ensureFreshSession()` wired into `class-management.di.ts` real branch. **Major finding**: IAM's public API has no `GET` member-listing or single-lookup endpoint at all (only `POST`/`PATCH`/`DELETE`) — the epic's assumed "teacher list đổi sang IAM members" fix is not achievable; `listTeachers` stays mock-first permanently, unchanged from before this US (cross-repo ask #7 logged in EPIC-OVERVIEW.md, plus ask #8 for putting the 3 derived fields on `ClassResponse`). Minimal compile-compat fix applied to the sibling `principal-teachers.repository.ts` (reuses the same mapper; call site updated to the new signature with a documented known-gap comment, no behavior change — that repo's real mode was already non-functional before this US). Zero UI/ViewModel change. | yes | yes | no | yes | implemented | `class-management.mapper.test.ts` (6, new — id/field renames, enrichment injection, request-body shaping, homeroom name fallback); `class-management.repository.test.ts` (rewritten, ~34 tests — full error matrix incl. 5 new failures, fan-out enrichment for listClasses incl. pagination-to-completion + client-side gradeLevel filter, renameClass backfill-then-patch + re-enrichment, getHomeroomTeacher 404-as-null, raw-flag placement + real-interceptor regression guard); 287 files / 1712 tests pass (zero regression, baseline 286/1680); `bunx tsc --noEmit` clean; `bun run build` green; `bun lint` clean; tech-lead review APPROVED (independently re-verified all contract claims against `edu-api/services/{iam,core}/docs/` — no findings required a fix) |
| US-E18.7 | Assessment scheme + grade scale wiring: path fix (`/config/` prefix dropped from grade-scale, trailing `/terms/{termId}` added to assessment-schemes) plus DTO remap once real drift found — separate Request/Response wire schemas, `coefficient`↔`weight` unit scaling (÷10 / ×10, lossless for schemes this UI creates), `ordinal` derived from array order, grade-scale `bands` derived from real `letterGrades` for `LETTER_ABCD` else falls back to local `GRADE_SCALE_PRESETS` (BE has no numeric-scale banding concept), `count` non-persistent (zero wire representation). Ground-truthed error taxonomy from Go source (`services/core/internal/assessment/core/domain/error/config.go`) confirms decision 0008 UPPER_SNAKE holds for `core` — 9-code matrix replacing 3 guessed-never-matched codes. Adds minimal `["HK1","HK2"]` term selector (reuses existing `Select` pattern) since BE requires `termId`. `listSubjectsForGrade` stays mock (real `GET /subjects` has no gradeLevel filter — belongs to US-E18.3). `grades` feature's domain reuse of `AssessmentScheme`/`GradeScale` kept unchanged (zero ripple, compile-only `termId`/`effectiveFrom` literal additions). ADR 0053. | yes | yes | yes | yes | implemented | `assessment-scheme.mapper.test.ts` (rewritten — scaleType round-trip, letterGrades/preset band derivation, coefficient/weight scaling, ordinal-from-index, count non-persistence); `assessment-scheme.repository.test.ts` (rewritten — full 9-code error matrix + request-body shape assertions + termId path threading); `mock-assessment-scheme.repository.test.ts` (updated — term-scoped key); 4 new Storybook interaction stories for the term selector (`TermNotSelected`/`TermHK1Selected`/`TermHK2Selected`/`SchemeLoadError`); 289 files / 1751 vitest pass (baseline 288/1725, zero regression, independently re-run by reviewer); `bun vitest:storybook run src/features/assessment-scheme` 10/10 pass; `bunx tsc --noEmit` clean; `bun run build` green; `bun lint` clean; tech-lead review APPROVED (independently re-verified error matrix/DTO shapes/scaling against edu-api Go source); a11y audit PASS (0 blocking, 2 pre-existing minor notes routed elsewhere, not this story's scope); design-review gate PASS (no new tokens, 1:1 reuse of existing Select pattern). |
| US-E18.8 | Staff-leave wiring: the "add `/conduct/` segment" label held at the path level only. Real finding: `GET` requires a mandatory `staffMemberId` query param (no tenant-wide oversight list exists — the admin screen lists every staff member's requests at once) AND `StaffLeaveRequestResponse` has zero display fields (no `staffName`/`department`/`leaveType` — the leave-type concept itself doesn't exist on the wire). `approve`/`reject` are therefore also unreachable (their only id source is the blocked list). Whole feature stays mock-first permanently — first fully-blocked DI factory in the epic (`staff-leave.di.ts` force-mocks regardless of `USE_MOCK`, vs. the hybrid pattern used by US-E18.4/US-E18.5). Ground-truthed 7-code error taxonomy (confirmed UPPER_SNAKE from Go source `pkg/kit/response/error.go`'s `codeFromKey`) kept correct + unit-tested for the day this unblocks; 2 new failure types (`forbidden`, `same-actor`) added with vi/en i18n keys. Zero UI/ViewModel change. Cross-repo ask #13 logged. | yes | n/a | n/a | yes | implemented | `staff-leave.repository.test.ts` (new, 12 tests — full 7-code `toFailure` matrix + network-error fallback + 3 stub-never-calls-http guards); 290 files / 1763 tests pass (zero regression vs baseline 289/1751); `bunx tsc --noEmit` clean; `bun run build` green; `bun lint` clean; no UI/Storybook/E2E change (existing screen tests pass unchanged); tech-lead review APPROVED (independently re-verified GET's mandatory `staffMemberId`/no-tenant-list, zero display fields, and the 7-code error matrix against edu-api Go source — 1 SHOULD-FIX finding: `forbidden` was mapped from `LEAVE_REQUEST_FORBIDDEN`, real code for list/approve/reject is `VIOLATION_FORBIDDEN`; fixed same-commit, both codes now map). |
| US-E18.9 | Teaching-plan wiring: the epic table's "nest `/lms/` + decide `/cells`" label understated the drift — ground-truthing `edu-api/services/core`'s `TeachingPlan (LMS)` contract + Go source found three independent blockers, not one path fix: (1) composite-key granularity mismatch — web keys a plan by `(subjectId, classId, term)`, real key is `(classSubjectId, academicYear, planId)` with no term dimension (one BE plan spans a full academic year); (2) no period axis on the wire (`WeeklyEntryResponse` = `{weekNumber, topic, notes}` only, web grid is week × period); (3) **THE `/cells` DECISION**: zero HTTP surface to edit an existing plan's entries at all — `create` sets `weeklyEntries` once, no `PUT`/`PATCH` afterward, and the domain aggregate's `UpdateEntries()` method exists+is unit-tested BE-side but is dead code, never wired to any route. Whole feature stays mock-first permanently — second fully-blocked DI factory in the epic after US-E18.8's `staff-leave.di.ts`. Ground-truthed 6-code error taxonomy (confirms UPPER_SNAKE for `core`) replaces the old guessed taxonomy that matched zero real codes; no new `TeachingPlanFailure` types needed. `TEACHING_PLAN_EP` updated to real nested paths for documentation (kept for the day this unblocks); `cells` endpoint constant removed (no wire equivalent). Zero UI/ViewModel change. Cross-repo ask #14 logged. | yes | n/a | n/a | yes | implemented | `teaching-plan.repository.test.ts` (extended — full 6-code `toFailure` matrix + unknown fallback + 6 stub-never-calls-http guards, one per interface method, alongside pre-existing unchanged `MockTeachingPlanRepository` behavior tests); 290 files / 1777 tests pass (zero regression vs baseline 290/1763, +14 new); `bunx tsc --noEmit` clean; `bun run build` green; no UI/Storybook/E2E change (existing screen tests pass unchanged); tech-lead review verdict recorded in story packet. |
| US-E18.10 | Class-log wiring + `revise` workflow state: path prefix + DTO already correct (rare zero-drift cluster). Two real gaps closed — (1) missing `revise` transition: BE state machine is `DRAFT→SUBMITTED→APPROVED`/`→REJECTED→SUBMITTED (revise)`; `submit` only accepts `DRAFT` BE-side, a REJECTED entry must go through the separate `POST .../revise`. Added `reviseEntry` repo method + `CLASS_LOG_EP.revise` + `ReviseEntryUseCase` + `makeReviseEntryUseCase` DI + real teacher `reviseEntryAction` (principal stub returns `unauthorized`) + distinct "Revise & resubmit" detail action for REJECTED (split `canTeacherSubmit`/`canTeacherRevise`, rejection banner stays visible). (2) Guessed error taxonomy never matched the wire: rewrote `toFailure()` to the 5 ground-truthed UPPER_SNAKE codes from `internal/school/core/domain/error/homeroom.go` (`HOMEROOM_ENTRY_NOT_FOUND`→`not-found`, `HOMEROOM_ENTRY_ALREADY_EXISTS`→`already-exists`, `HOMEROOM_INVALID_TRANSITION`→`invalid-transition`, `HOMEROOM_SUMMARY_REQUIRED`→`summary-required`, `HOMEROOM_FORBIDDEN`→`forbidden`); `ClassLogFailure` union renamed (`already-submitted`/`not-submitted`/`duplicate-date` removed). `CreateEntryUseCase` now throws `{type:"summary-required"}`. Mock repo models the real state machine (duplicate-date create guard, submit only-from-DRAFT, revise only-from-REJECTED, approve/reject only-from-SUBMITTED). `approve`/`reject`/`revise` actions now return `{ok:true,entry}` (screen uses server-authoritative entry, no client-fabricated status patch). `ensureFreshSession()` wired into `class-log.di.ts` `!USE_MOCK` branch (first time). `getEntry`-by-id intentionally not wired (no UI consumer). i18n `classLog.errors`/`detail` keys added vi+en. | yes | yes | yes | yes | implemented | `revise-entry.use-case.test.ts` (new); `class-log.mock.repository.test.ts` (new, 9 tests — duplicate-date create, submit/revise/approve/reject transition guards); `create-entry.use-case.test.ts` (updated for `summary-required`); `class-log.repository.test.ts` (5-code error matrix rewrite + `reviseEntry` happy path, raw-flag regression guard untouched); 292 files / 1790 tests pass (zero regression vs baseline 290/1777, +2 files +13 tests); new Storybook interaction story `TeacherReviseRejectedEntry` (REJECTED shows "Revise & resubmit" not "Submit", click transitions to SUBMITTED, rejection reason stays visible) + existing stories updated for entry-returning approve/reject; `bunx tsc --noEmit` clean; `bun run build` green; `bun lint` clean (class-log files). |
| US-E18.19 | Sweep raw-flag placement (bug class first found in US-E18.2): `isRawCall` in `bootstrap/lib/api-envelope.ts` reads `config.raw` at the axios config TOP LEVEL, not nested inside `params`. Grepped `raw: true` across `src/features` (excl. test/mock) and found 9 more call sites in 7 repos still nesting it inside `params` — `principal-teachers.repository.ts` (2), `class-log.repository.ts` (1), `subject-catalogue.repository.ts` (2), `class-management.repository.ts` (1), `admin-roster/roster.repository.ts` (2 — both `getClasses` and `getClassRoster`, not just one as initially assumed), `teacher-class.repository.ts` (1, `fetchAllPages`), `teacher-dashboard.repository.ts` (1, `fetchAllPages`) — hoisted `raw: true` to a sibling of `params` in each, no query-param values touched. Verified `notification.repository.ts` and `audit-log.repository.ts` were already correct (top-level spread) and added protective regression guards only (no source change). Fresh grep after the sweep confirms no repo under `src/features` still nests `raw` inside `params`. | yes | no | no | yes | implemented | Regression-guard `describe("… real interceptor pipeline (raw-flag placement)")` added to all 9 repos' test files (7 fix + 2 verify-only), piping the mocked `http.get` response through the REAL `unwrapResponse` from `@/bootstrap/lib/api-envelope` — red against the old nested-raw code, green after each fix; 2 new test files (`class-management.repository.test.ts`, `teacher-dashboard.repository.test.ts`); 246 files / 1357 tests pass (zero regression); `tsc --noEmit` clean; `bun run build` green; tech-lead review APPROVED (no scope creep, fresh grep confirms sweep complete). |
| US-E18.11 | Timetable wiring (builder + consumer views): admin builder GET/PUT(read-modify-write, real BE has no per-slot PUT)/DELETE-slot wired real against `core`'s class-scoped `/classes/{id}/timetable(/slots)`; reactive `TIMETABLE_TEACHER_CONFLICT` 409 surfaced as new `teacher-conflict` failure through the existing save-error toast; whole-school `getConflicts` stays mock permanently (no bulk BE endpoint, ask #16); consumer `getByTeacher` wired real via `GET /classes` teacher-auto-filter fan-out + merge (reusing the `teacher-class.repository.ts` precedent); consumer `getByClass` ALSO stays mock (implementation-time correction — its only caller, the parent flow, is itself permanently blocked); `getMyTimetable` (student) + `getChildren`/child view (parent) stay mock permanently — no self-scope discovery endpoint exists for STUDENT/PARENT roles (ask #15, ground-truthed against Go `list_classes.go` 403 for non-ADMIN/non-TEACHER). Full 11-code error taxonomy; day-enum bridge (Mon–Fri only); term resolved via already-real `calendar` (US-E18.1). Zero UI/ViewModel change. See story packet for full remap. | yes | yes | n/a | yes | implemented | `day-enum.test.ts` (both features), `resolve-term.test.ts`, `timetable.mapper.test.ts`, `real-weekly-timetable.mapper.test.ts` (new units); `timetable.repository.test.ts`, `real-weekly-timetable.repository.test.ts` (new integration — full error matrix, RMW-PUT, real DELETE, 409→teacher-conflict, fan-out+merge, raw-flag guard, force-blocked stub guards); 299 files / 1837 tests pass (zero regression vs baseline 292/1790, +7 files/+47 tests); `bunx tsc --noEmit` clean; `NEXT_PUBLIC_USE_MOCK= bun run build` green; `bun lint` clean; no UI/Storybook/E2E change (pre-existing unrelated Storybook harness gap in `timetable-screen.stories.tsx`, confirmed present on baseline `main` too via `git stash`); tech-lead review APPROVED (independently re-ground-truthed the full contract against edu-api Go source; 2 non-blocking findings fixed same-commit — teacher-filter decoupled from the display-name fallback, null-currentUserId now throws not-found instead of returning empty).
| US-E18.14 | Discipline → conduct remap: ground-truth audit found the whole feature (student-violations, student-conduct-grades, student-leave-requests) is blocked by two compounding gaps — no real roster UUIDs for admin/teacher-authored records (extends ask #9), and a NEW finding that the classId self-discovery gap (ask #15, previously scoped to PARENT) also blocks STUDENT self-view (`list_student_violations.go`/`list_student_conduct_grades.go` require `classId` even for the own-record branch). Repository/DTO/error-taxonomy/DI-only remap (mirrors the US-E18.8 staff-leave force-mock pattern): `DISCIPLINE_EP` remapped to the real `/core/api/v1/conduct/student-*` paths (recorded, not consumed); `DisciplineFailure` extended with 4 new ground-truthed types (`same-actor`, `invalid-transition`, `locked` [ADR 0074], `student-not-enrolled`) + vi/en i18n keys; `toFailure()` rewritten to the full 19-code matrix (branch on code); `DisciplineRepository` every method is a permanent blocked stub (never calls `http.*`); `discipline.di.ts` force-mocks regardless of `USE_MOCK`. Zero UI/entity/use-case/mock-repo/fixtures change (zero-regression AC). `staff-violations`/`staff-conduct-notes`/`student-absences` have no web screen at all — descoped as a product gap, not a BE gap. See story packet. | yes | n/a | n/a | yes | implemented | `discipline.repository.test.ts` extended (20 toFailure matrix assertions incl. the 4 judgment-call reuses + legacy `LEAVE_ALREADY_DECIDED`→already-processed + unknown-code fallback; 18 permanent-blocked-stub guards asserting each method throws without touching `http.*`); all existing `MockDisciplineRepository`/parent-multi-child tests kept unchanged; 303 files / 1902 tests pass (zero regression vs baseline 303/1866, +36 tests, same file count); `bunx tsc --noEmit` clean; `bun lint` clean (discipline files); `bun run build` green. No UI/ViewModel/Storybook/E2E change. tech-lead review APPROVED (independently re-ground-truthed the force-mock premise against edu-api Go source incl. the mandatory-`classId` claim on the STUDENT-self branch — confirmed real, no live real-branch contradicts the stub; independently re-ran full suite/tsc/build; 0 blocking findings, 2 non-blocking CONSIDER notes on a stale comment + 2 error-message-length judgment calls, addressed same-branch). Design-review + a11y gates not triggered (zero UI surface, mirrors US-E18.8). |
| US-E18.13 | Academic-records seal remap: `sealBatch` wired real (bare `POST /core/api/v1/classes/{id}/terms/{id}/academic-records/seal`, no request body) via a new `HybridAcademicRecordsSealRepository` facade (real for `sealBatch`, mock for the other 8 seal/unseal methods); hard client pre-check (`getSealStatus`) removed — `SealAcademicRecordUseCase` is now a thin pass-through, gate moved to reactive real failures `unlocked-grades-exist` (422) / `too-many-reseals` (422, cap 5); reseal is idempotent (dropped old blocking `already-sealed`). Unseal workflow (`initiateUnseal`/`confirmUnseal`/`getPendingUnsealRequests`/`listTenantAdmins`) stays permanently mock — real BE has POST create+approve but NO GET listing endpoint for pending requests at all, so a real requestId can never be discovered by a second admin (cross-repo ask #21, ADR 0055); viewer (`getRecord`/`listYears`) stays mock — no wire year-grouping, no fixed tx1/tx2/giuaKy/cuoiKy columns (real snapshot is dynamic column array per US-E18.7), no student-identity fields on this endpoint. `AllLockedGate` NOT-OK branch now shows an actionable Seal button alongside the warning (decorative-not-blocking); reseal button label switches. `ensureFreshSession()` wired into the real branch. Ground-truthed 9-code `ACADEMIC_RECORD_*`/`UNSEAL_REQUEST_*` error taxonomy (UPPER_SNAKE, confirmed via `codeFromKey`). See story packet + ADR 0055 (amended). | yes | yes | yes | yes | implemented | `seal-academic-record.use-case.test.ts` (getSealStatus-never-called spy, idempotent reseal, bubbles unlocked-grades-exist/too-many-reseals/forbidden); `academic-records-seal.repository.test.ts` (new — bare-POST-no-body assertion, result mapper, 6-code error matrix, dormant-method guard); `academic-records-seal-hybrid.repository.test.ts` (new — spy-count routing, sealBatch→real only, 8 others→mock only); `academic-records-seal.mock.repository.test.ts` (idempotent reseal, reactive unlocked-grades-exist, resealCount cap→too-many-reseals); 303 files / 1866 tests pass (baseline 301/1852, zero regression, +2 files/+14 tests); Storybook interaction 109/126 files, 668/739 tests — same 17 pre-existing-failure files as before (unrelated harness gap), seal-screen file 17/17 (incl. new `AllLockedGate_NotOK_Mobile375` real-viewport proof added by QA); `bunx tsc --noEmit` clean; `bun lint` clean; `NEXT_PUBLIC_USE_MOCK= bun run build` green; tech-lead review APPROVED (0 blocking); a11y audit PASS after 1 should-fix (A11Y-001, role=alert scoping) fixed same-branch; design-review gate PASS (scoped self-review, no new tokens/screen); QA gate GO (independently re-verified all claims, closed 1 mobile-layout test gap). |
| US-E18.12 | Grades contract remap: retired invented `classSubjectId` for real `(classId,subjectId,termId,academicYearLabel)`; row-level `publishStatus` → per-cell `GradeEntryStatus` (`deriveRowStatus` pure derivation for roster-scanning, never stored); `publishGrades` (bulk) → per-cell `submit` fan-out (`SubmitColumnScoresUseCase`, sequential, never short-circuits, full partial-failure aggregation — per-cell `aria-invalid` retry indicator, A11Y-101); term `lock` wired real (irreversible, `DestructiveConfirmDialog` reused, `errorSlot` retry path, A11Y-102); student/parent self-view wired real via `GET /members/{id}/grades`. `IGradeApprovalRepository` (admin batch dashboard) force-mocked permanently (fixed a live real-branch that contradicted this claim — tech-lead blocking finding) + parent child-switcher (`get-child-list`) stay permanently mock — no wire batchId/rollup/display-name source (ADR 0054, cross-repo asks #18–#20). New `resolve-my-grade-subjects.ts` composes real class-subject picker from already-wired `class-management`/`teacher-class`. `ensureFreshSession()` wired into every real branch. 11-code ground-truthed error taxonomy. See story packet for full contract + evidence. | yes | yes | yes | yes | implemented | `derive-row-status.test.ts`, `submit-column-scores.use-case.test.ts` (2/5-fail partial-aggregation, sequential no-short-circuit), `lock-term.use-case.test.ts`, `grades.mapper.test.ts`/`grade-book.mapper.test.ts`, `grades.repository.test.ts`/`grade-book.repository.test.ts` (full 11-code error matrix + raw-flag placement), mock repos updated to model per-cell status truthfully; 301 files / 1852 tests pass (baseline US-E18.11 299/1837, zero regression); 13 new Storybook interaction tests (partial-submit-failure per-cell indicators, submit-button enablement, term-lock success/failure/forbidden dialog flows) verifying A11Y-101/A11Y-102 end-to-end — full interaction suite diffed byte-identical (71/71 pre-existing failures) against clean `main`, confirming `grade-approval-screen`/`child-switcher` genuinely untouched; `bunx tsc --noEmit` clean; `bun lint` clean; `NEXT_PUBLIC_USE_MOCK= bun run build` green; tech-lead review Revision Required → fixed → Approved; a11y audit 1 blocking + 4 non-blocking → all fixed, re-verified PASS; design-review gate PASS (no new tokens); QA gate GO. |
| US-E18.15 | LMS exam-bank contract remap: ground-truthing the Go source found `openapi.yaml`'s ExamBank write-path docs themselves drifted (create is metadata-only, `/questions` appends ONE DRAFT-only question with no options-text field, no update/delete endpoint exists at all) — ADR 0056 amended mid-implementation (Option A). Wired REAL: `listExamBank` (cursor-paginated, `raw:true` top-level, `subjectName` via subject-catalogue fan-out, `teacherName`→`authorId` fallback), `getExamDetail`, `publishExam` (status PUT, DRAFT→PUBLISHED). Admin exam-bank screen (read-only) now 100% real; teacher list+publish real. `createExam`/`updateExam`/`deleteExam` are permanently blocked stubs (hybrid DI, US-E18.5/13 precedent) — teacher "Create"/delete hidden (`authoringEnabled=USE_MOCK`), builder routes (`create`/`[id]/edit`) render new `ExamBuilderUnavailable` state in real mode instead of a form that would fail on submit — flagged/reviewed as UI-behavior scope beyond "hide delete only". 3-value status (DRAFT/PUBLISHED/CONFIDENTIAL) via shared `StatusBadge`. 13-code UPPER_SNAKE `ExamBankFailure` matrix ground-truthed against `exam_paper.go`. `validateQuestion` fixed to tolerate real questions' absent `options` array. `ensureFreshSession()` wired into the real DI branch. `src/features/exam` (student exam-taking) + the entire `/lms/class-exams*` family (publish-to-class, admin activate/complete/retract, submissions) stay permanently mock — no existing UI, no self-scope `classId` resolver for STUDENT, essay submission has zero wire representation (cross-repo asks #23-26, ADR 0056 Amendment). See story packet + ADR 0056. | yes | yes | yes | yes | implemented | `exam-bank.mapper.test.ts`, `exam-bank.repository.test.ts`, `map-exam-bank-error.test.ts` (13-code matrix), `validate-questions.test.ts`, `publish-exam.use-case.test.ts` (extended — real repo-thrown errors flow to typed failure output); 307 files / 1950 tests pass (baseline 303/1902, zero regression, +4 files/+48 tests); Storybook interaction 14/15 in `exam-bank` (1 pre-existing failure, `Builder Validation`/`aria-disabled` vs `toBeDisabled`, confirmed failing identically on the pre-US baseline via an isolated worktree — logged as a follow-up, not this US's regression); new stories: dropdown-opened assertion that Edit/Delete are genuinely absent (not disabled) while Publish stays present, `ExamBuilderUnavailable` sr-only heading (A11Y-201 fix) + back-button, `confidential` 3rd status value with distinct label/tone, 320px responsive card-grid guard; `bunx tsc --noEmit` clean; `bun lint` clean; `NEXT_PUBLIC_USE_MOCK= bun run build` green; tech-lead review APPROVED (independently re-verified against Go source incl. blocked-stub unconditional-throw check, no live real-branch contradicting the claim); a11y audit PASS after 1 should-fix + 2 minor fixed same-branch, re-verified; design-review gate PASS (scope-creep beyond original framing explicitly disclosed); QA gate GO (wrote new coverage, didn't just review self-report; confirmed zero regression on out-of-scope `src/features/exam` via empty diff). |
| US-E12.2 | Academic Calendar: year/term CRUD, date-order validation, date-overlap validation, graded-term delete block, mock-first DI, accordion UI | yes | yes | no | yes | implemented | create-term (5), update-term (4), delete-term (3), create-year (4) unit tests (16 total); mock repository integration; bun build + tsc clean; a11y blocking issues fixed; design-review pass |
| US-E07.2 | --primary semantic var → --edu-primary-dark (#4570EA); WCAG AA 4.56:1 on white; all bg-primary/text-primary-foreground fixed globally via token remap | no | no | no | yes | implemented | No domain logic — pure CSS token change. tsc --noEmit clean (0 errors); bun build green; biome 0 issues; 130/130 Vitest tests pass; contrast ratio 4.56:1 verified (≥4.5:1 AA). ADR 0023 Accepted. |
| US-E07.5 | A11Y deferred cleanup (E07.3/E07.4): StatCard trend chip text-edu-success/error → text-edu-success-text/error-text (WCAG 1.4.3, 4.5:1 for text-xs); SecurityTab new-password Label+Input linked via useId() htmlFor/id (WCAG 1.3.1/4.1.2) | yes | no | no | yes | implemented | trendColorClass() unit tests (2 new, 7 total stat-card); 160/160 Vitest pass (28 files); tsc --noEmit clean; next build green. Decision 0027. |
| US-E12.7 | A11Y-006 sweep: text-edu-primary on normal text (non-icon, non-large-text) must achieve WCAG AA ≥4.5:1; icons/large-text confirmed passing at ≥3:1 | no | no | no | yes | implemented | 1 fix: calendar-screen.tsx:509 text-edu-primary → text-edu-primary-dark (4.56:1); 10 occurrences swept; tsc clean; 130/130 Vitest pass; bun build green; design-review pass |
| US-E12.8 | Admin route role guard: server-side RSC layout enforces role==="admin" before any /admin/* content renders; non-admin → redirect to DEFAULT_ROUTE; no token/unknown role → redirect to select-tenant; mock-first (NEXT_PUBLIC_USE_MOCK=true, NODE_ENV!==production) | yes | no | no | yes | implemented | decodeRoleClaim (8 tests), evaluateAdminAccess (7 tests); 145/145 Vitest pass; tsc --noEmit 0 errors; bun build green; tech-lead Approved; a11y Pass (no UI rendered) |
| US-E12.3 | Subject Catalogue: SubjectParent departments CRUD + archive (block when activeChildCount>0), grade-scoped Subjects per department with master-detail 35/65 layout, Subject detail sheet (locked curriculum fields, class offerings), code validation /^[A-Z0-9]{1,16}$/, mock-first DI (core service absent), concept badge AA tokens (decision 0029) | yes | yes | no | yes | implemented | validateSubjectCode (5), ArchiveParent (2), ArchiveSubject (2), CreateSubject (3) = 12 use-case unit; MockSubjectCatalogueRepository (5 integration: create parent, create subject, archive parent, archive subject, getSubject bundle); 194/194 Vitest pass; tsc clean; bun build green. Routes: /admin/subject-departments, /admin/subjects. |
| US-E12.4 | Student roster / class enrollment: class selector breadcrumb, roster table (search, bulk select, pagination, transferred muted+strikethrough), Add panel (transfer-warning, enroll/transfer), unenroll + transfer confirm dialogs, empty state; mock-first DI; one student ⇒ one class per year invariant (enroll-with-currentClassId ⇒ transfer) | yes | yes | yes | yes | implemented | enroll-student.use-case (3: unassigned/transfer-warning/failure), unenroll-student.use-case (2: ok/not-found) = 5 unit; roster.mapper (6) + roster.repository (6: envelope unwrap, 404→not-found, 401→unauthorized, paths) = 12 integration; 6/6 Storybook interaction stories pass (Loading/EmptyClass/Populated/TransferWarning/BulkSelected/ErrorState); 194/194 Vitest pass; 110/110 Storybook tests pass (44 files); tsc --noEmit clean; bun build green. ERR_REQUIRE_ESM unblocked by INFRA-storybook-test-runner (ADR 0032). |
| US-E12.5 | Timetable builder: weekly grid (Mon–Sat × 10 periods + lunch recess), filled/empty slot cells, SlotEditor dialog (subject select → teacher select filtered by TeachingAssignment per ADR 0029 → room), school-wide teacher conflict detection (pure domain fn), conflict summary panel with jump-to-cell, class/year selectors (searchParams nav), Export PDF / Import placeholder toasts; mock-first DI (core absent); admin route guard (inherited AdminLayout) | yes | yes | no | yes | implemented | detectConflicts (7 unit: empty/single/diff-day/diff-period/2-class/3-class/2-teacher), update-slot (4: delegate/trim/missing-subject/missing-teacher), clear-slot (1) = 12 unit; MockTimetableRepository (4 integration: seed=3 conflicts, assign→conflict appears, clear→conflict resolves, getTimetable class-scoped slots + school-wide conflicts) = 16 new tests; 223/223 Vitest pass (43 files); 4 Storybook stories authored (Default/Empty/NoConflicts/WithSlotEditorOpen); tsc --noEmit clean; bun build green (route /admin/timetable). |
| US-E12.10 | Class Management UI: classes table (name/grade/GVCN/status/student-count), create/rename sheets (grade clamped to school range), archive AlertDialog (client-side studentCount>0 warning), homeroom picker sheet (client-side teacher search); mock-first DI (core absent; listTeachers always mock); grade-range validation in CreateClassUseCase | yes | yes | no | yes | implemented | create-class (4: valid no-range / valid in-range / grade-out-of-range / duplicate propagation), rename-class (2: rename / not-found), assign-homeroom (2: assign / not-found) = 8 use-case unit; all class-management tests pass; tsc --noEmit 0 errors; bun lint clean (1 pre-existing warning in admin-roster, untouched); 4 Storybook stories authored (Empty/Populated/WithHomeroomAssigned/ArchiveWithWarning). Route /admin/classes; nav-config admin + classManagement i18n (vi+en). |
| US-E07.6 | Pagination active-state accessible token: --edu-primary-accessible (#4468e0) = 4.88:1 on white, closes A11Y-005; applied to roster-pagination.tsx active button; tokens.css + globals.css + design-system.md synced; ADR 0031 Accepted | no | no | no | yes | implemented | No domain logic — pure CSS token + component fix. 194/194 Vitest pass; tsc --noEmit clean; bun build green. Contrast verified: #4468e0 on white = 4.88:1 (≥4.5:1 AA). |
| US-E07.7 | A11y hardening DR-001→DR-007: A11Y-050 principal-review approve/reject buttons contextual aria-label (subject+class); A11Y-051 lesson-bank-skeleton i18n compliance (hardcoded Vi strings → typed next-intl keys); A11Y-052 exam-builder aside+section landmark aria-label. 7 other flagged items verified already-covered by Radix/shadcn and global reduced-motion reset. | yes | no | no | no | implemented | 804/804 Vitest pass (159 files); tsc --noEmit clean; bun build green. No new tokens needed. |
| US-E08.4 | html lang derived from [locale]: RootLayout uses getLocale() from next-intl/server to set lang={locale} dynamically; closes A11Y-012; vi routes → lang="vi", en routes → lang="en" | no | no | no | yes | implemented | No domain logic. 194/194 Vitest pass; tsc --noEmit clean; bun build green. |
| INFRA-storybook-test-runner | Storybook interaction-test runner (vitest:storybook) executes play() fns without ERR_REQUIRE_ESM; all 110/110 Storybook interaction tests pass. Fix: bun patch @storybook/nextjs-vite@10.4.2 (dynamic import() instead of require()), postcss.config.mjs → .js (CJS), story decorators (nextjs.appDirectory + NextIntlClientProvider). | no | no | yes | no | implemented | 110/110 Storybook interaction tests pass (44 files); 194/194 unit tests pass; tsc --noEmit clean; bun build green. ADR 0032. |
| US-E06.3 | Kong gateway base URL: NEXT_PUBLIC_API_URL default → localhost:8000; all core endpoint files add /api/v1 segment; all IAM endpoint paths get /iam/api/v1 prefix; bun build + tsc clean | no | yes | no | yes | implemented | 194/194 Vitest pass (37 files); tsc --noEmit clean; bun build green (19 routes). Endpoint tests: roster.repository (6 — path assertions updated to /core/api/v1/*), auth.repository (3 — AUTH_EP refs follow updated constants). ADR 0030 Accepted. |
| US-E06.4 | IAM tenant/member/invitation real wiring: IamMemberRepository (inviteMember, removeMember, changeRoles, acceptInvitation, listMyTenants, switchTenant); token rotation on switch-tenant; last-admin invariant error mapping | yes | yes | no | yes | implemented | 10 new repo tests (8 failure-code mapping + 2 happy paths); 233/233 total pass (44 files); tsc clean; bun build green (19 routes). IamMemberRepository maps FORBIDDEN_ACTION, USER_EMAIL_ALREADY_EXISTS, INVITATION_NOT_FOUND+EXPIRED, LAST_ADMIN_INVARIANT_VIOLATION, RESOURCE_NOT_FOUND, NETWORK_ERROR → typed failure union. switchTenant returns AuthTokens via mapTokens. |
| US-E06.5 | core school-config + academic-calendar real wiring: SchoolConfigRepository lifted from mock; CalendarRepository (years + terms CRUD, cursor-pagination, date-overlap, archive-blocked); Kong /core/api/v1/* | yes | yes | no | yes | implemented | 13 new repo tests (8 calendar + 5 school-config); 223/223 total pass (43 files); tsc clean; bun build green (19 routes). CalendarRepository: activate/archive contract, cursor-paginated listYears (raw+parseEnvelope), errorCodeOf mapping. SchoolSetupFailure + CalendarFailure failure unions expanded. |
| US-E06.6 | core subject-catalogue real wiring: SubjectCatalogueRepository error-code mapping (SUBJECT_PARENT_IN_USE, SUBJECT_IN_USE, SUBJECT_GRADE_LEVEL_OUTSIDE_TENANT_RANGE, CLASS_SUBJECT_LOCKED_FIELD_UPDATE, etc.); cursor-pagination via raw+parseEnvelope; expanded SubjectCatalogueFailure union | yes | yes | no | yes | implemented | 12 new repo integration tests (error-code mapping + paginated list); 245/245 total pass (45 files); tsc clean; bun build green. i18n: 10 new error keys in vi+en. |
| US-E06.7 | core student-roster real wiring: RosterRepository two-step transfer (DELETE+POST); ROSTER_STUDENT_ALREADY_ENROLLED=already-enrolled; idempotent unenroll; ROSTER_ACCESS_FORBIDDEN→forbidden; academicYear param; cursor-pagination; search pool mock-first | yes | yes | no | yes | implemented | 14 repo integration tests (error-code mapping + two-step transfer + paginated list); 252/252 total pass (45 files); tsc clean; bun build green. i18n: 4 new error keys in vi+en. |
| US-E06.8 | core staffing real wiring (new feature): StaffingRepository (departments, position-titles, position-assignments); MANAGE_SUBJECT_CONTENT permission scope constraint; copy-year-to-year; design-needed for screens | yes | yes | no | yes | implemented | 32 new tests (18 use-case unit + 14 repo integration error-code mapping); 284/284 total pass (52 files); tsc clean; bun build green. Staffing screens added to screens.md as planned. |
| US-E01.2 | Login SSO (VNeID + Google) + multi-role/multi-tenant select: SSO buttons on login form; role-select step for users with ≥2 roles; ROLE_META mapping (6 roles enum→appRole); Google OAuth wired; VNeID aria-disabled / coming-soon (ADR 0035); roleEnum preserved on UserTenantRole (ADR 0036) | yes | yes | yes | yes | implemented | Unit: 3 SocialAuthUseCase + 5 RoleSelectUseCase (TDD red→green, incl. multi-tenant same-appRole case); Integration: 3 AuthRepository.socialSignin (success + UNAUTHORIZED + network); E2E/Story: 6 Storybook stories (LoginWithSSOButtons, SSOGoogleLoading, SSOError, RoleSelect TwoRoles/ThreeRoles/SingleRole) with play() assertions; 304/304 pass (56 files); tsc clean; bun build green; WCAG 2.1 AA: contrast + focus ring + heading + role card aria-label all fixed; design-review gate passed. |
| US-E13.4 | Teacher Dashboard Home: 5 StatCards (student count real BE; rest mock-first), today's schedule by Tiết 1–10, pending grades list with "Nhập điểm" CTA, notifications list | yes | no | yes | yes | implemented | 7 unit tests (use-case parallel fan-out + failure propagation + mapper tones); Storybook: 6 stories (Default/AllStats/EmptySchedule/Loading/PendingGradesDetail/NotificationsDetail) with play() assertions covering all 19 ACs; 291/291 pass (54 files); tsc clean; bun build green. |
| US-E13.5 | Principal Teachers Management: teacher list (GVCN badge, subject assignments), TeacherAssignmentSheet (GVCN picker + GVBM rows + subject availability filter + conflict flag); mock-first write | yes | yes | yes | yes | implemented | Unit: 22 tests across 5 use-cases (GetPrincipalTeachers, GetPrincipalClasses, GetClassSubjects, AssignHomeroomTeacher, AssignSubjectTeacher) + 3 mapper tests; Integration: 11 repository tests (assignHomeroomTeacher: success+403+404+409+network, assignSubjectTeacher: success+403+409, listTeachers, getClassSubjects) + 5 action tests; E2E/Story: 7 Storybook stories (TeacherList_Loading, _Populated, _Empty, _Error, AssignmentSheet_Open, _WithConflict, _Save_Loading) + overflow fixture; 379/379 Vitest pass (75 files); tsc --noEmit clean; bun run build green (/principal/teachers in output); WCAG 2.1 AA: A11Y-001–006 all fixed (StatusBadge primary contrast, avatar contrast, conflict icon role=img, SheetContent aria-describedby, TableHead scope=col, tooltip tabIndex); design-review gate passed. |
| US-E08.6 | SSE Connection Status UI: disconnect banner (warning-family tokens, auto-reconnect 4s via schedule-reconnect.ts, manual reconnect CTA, aria-live="polite" role="status", focus-managed on reconnect); pending-message floating pill (visible outside /messages, "99+" overflow, navigate on click, hidden in messaging section); message.new realtime event contract; mock-first SSE state (no BE emitter yet) | yes | yes | yes | yes | implemented | 224/224 Vitest files, 1193/1193 tests pass (49 new: 5 event, 4 schedule-reconnect, 19 sse-connection controller, banner+pill unit/a11y tests, 9 AppShell container tests); tsc --noEmit 0 errors; bun lint clean; bun build green; 6 Storybook interaction stories (Banner_Hidden/Disconnected/Reconnecting, Pill_Visible/Overflow/HiddenInMessages); fe-tech-lead-reviewer Approved; fe-accessibility-auditor found 2 blocking + 1 major (contrast on --edu-warning-text bold-only token misuse, focus lost on reconnect) fixed in commit 927b5f4; fe-qa-playwright Go (12/12 AC traced, closed AppShell container coverage gap); impeccable detect.mjs 0 anti-pattern hits; design-review pass. |
| US-E08.5 | Profile Linked Accounts: Account Requests sidebar card (informational, admin contact link); Linked Accounts section in Security tab (VNeID + Google link/unlink, linked/unlinked badges, optimistic state); mock-first write | yes | no | no | yes | implemented | unit=5 (link/unlink/get-linked-accounts use-cases); integration=0 (mock-first, no HTTP service yet); e2e=6 Storybook stories (profile-screen incl. linked/unlinked/loading states) with play() assertions; 341/341 Vitest pass; tsc --noEmit clean; design-spec.jsonc anchors #profile-linked-accounts + #profile-account-requests added (ADR 0034) |
| US-E12.6 | Grade Scale & Assessment Scheme Config: admin sets grade scale + assessment columns per subject per year; grade-scoped subject selector; onboarding step 4/5 | yes | yes | no | yes | implemented | Unit: 13 domain tests (8 validateGradeScale + 5 validateAssessmentScheme) + 4 mapper tests; Integration: 6 mock-repo tests (grade-scale CRUD, scheme CRUD, subjects by grade, default TT22); E2E: 0 (Playwright deferred); Stories: 6 Storybook stories (Loading, Empty, ErrorState, GradeScaleEditor, SchemeEditor, WeightValidationError); 651/651 Vitest pass (127 files); tsc --noEmit 0 errors; bun build green (/admin/assessment route); tech-lead APPROVED; A11Y-041–046 all resolved (weight-sum live region, count aria-describedby, skeleton accessible name, 44px touch targets, focus management, empty-state live region); design-review PASS. |
| US-E12.9 | Staffing UI: departments CRUD+archive, position-titles CRUD+archive, position-assignments (assign/revoke/copy-year); ADMIN write, all-member read; BE real (E06.8 wired); isAdmin RBAC gate; motion-safe a11y | yes | yes | no | yes | implemented | Unit: 32 use-case + mapper tests from E06.8 (create/archive department×2, create/archive positionTitle×2, assignPosition, copyAssignments); Integration: 14 repo tests from E06.8; Stories: 16 total — DepartmentsScreen (Populated/Empty/CreateError/ReadOnlyNonAdmin/ArchiveBlocked/CreateHappyPath play()), PositionTitlesScreen (Populated/Empty/CreateError/ReadOnlyNonAdmin), AssignmentsScreen (Populated/Empty/RevokeError/ReadOnlyNonAdmin/CopyYear/AssignHappyPath play()), StaffingScreen (Populated/AllEmpty); 337/337 Vitest pass (64 files); tsc --noEmit 0 errors; bun build green (/admin/staffing route). Tech-lead Approved; a11y all 3 Major findings fixed (motion-safe Sheet+AlertDialog, icon-lg 40→44px, aria-describedby blocked-archive). |
| US-E12.10 | Class Management UI: create/rename/archive classes; assign GVCN via picker; grade-level range validation; BE real (core classes API live) | no | no | no | no | planned | none |
| US-E13.1 | Teacher Class View: /teacher/classes grid (GVCN badge, student count, "Xem học sinh" + disabled Điểm danh/Sổ đầu bài with "Sắp có" tooltip), /teacher/classes/[classId]/students read-only roster (client search + pagination PAGE_SIZE=10, status badge, breadcrumb), dashboard "Lớp học" totalClasses StatCard (BookOpen); mock-first DI (USE_MOCK), GVCN from JWT sub vs homeroomTeacherId | yes | yes | no | yes | implemented | list-my-classes.use-case (3: ok/network/unauthorized), get-class-students.use-case (3: ok+classId fwd / empty-id→not-found / not-found) = 6 new use-case unit; teacher-class.mapper (4: map/displayName fallback/status default/transferred) + teacher-dashboard.mapper (4 incl. isHomeroom GVCN match/mismatch/absent/null) = 8 mapper unit; get-teacher-dashboard.use-case updated for totalClasses fan-out; 295/295 Vitest pass (55 files); Storybook: TeacherClassesScreen (Loading/Empty/WithClasses[GVCN badge=1, 4 cards]/ErrorState) + TeacherClassStudentsScreen (Loading/Empty/WithStudents[15→paginate+search]/ErrorState) with play() assertions; tsc --noEmit clean; bun build green; bun lint clean (1 pre-existing admin-roster warning untouched). Routes: /teacher/classes, /teacher/classes/[classId]/students. |
| US-E13.2 | Attendance BE Wiring: wire existing attendance UI to real core API (US-046 mock-first until BE ships) | no | no | no | no | planned | none — mock-first (BE US-046 planned) |
| US-E13.3 | Class Log Screen (Sổ đầu bài): teacher list+stats+entry form (draft/submit), principal review (approve / reject-with-reason), filter chips, detail view; REAL core homeroom-entries wiring (summary + notableEvents) + MockClassLogRepository fallback (USE_MOCK) | yes | yes | yes | yes | implemented | Unit: 6 use-case tests (CreateEntry valid + empty-summary-rejected-without-repo, SubmitEntry, ApproveEntry, RejectEntry with/without reason); Integration: 11 ClassLogRepository tests (list+pagination, create body, submit/reject endpoints, 7 error-code→failure mappings: NOT_FOUND/ALREADY_SUBMITTED/NOT_SUBMITTED/DUPLICATE_DATE/FORBIDDEN/NETWORK/unknown); E2E/Story: 7 Storybook stories with play() (TeacherListView[4 statuses]/TeacherNewEntryForm[required validation]/TeacherEntryDetail/PrincipalReviewView[no-create]/PrincipalApproveDetail/EmptyState/ErrorState); 397/397 Vitest pass (80 files); tsc --noEmit clean; bun lint clean (1 pre-existing admin-roster warning untouched). Routes: /teacher/class-log, /principal/class-log. |
| US-E14.2 | Grade Entry Screen: teacher inputs component scores per class-subject; coefficient-weighted average; publish/lock; BE real for assessment scheme (US-059); grade entry mock-first (BE US-060 planned) | yes | yes | yes | yes | implemented | Unit: calculateWeightedAverage (7 cases), validateScore (6), saveScore (4), publishGrades (4 incl. incomplete-scores guard), getGradeSheet (3) = 24 domain; Integration: grades.mapper (6), MockGradesRepository (6) = 12; Story: 8 Storybook stories with play() (Loading/NoSelection/WithScores/PublishConfirmDialog/PublishedReadonly/PendingApproval/ValidationError/EmptyClass); 687/687 Vitest pass (134 files); tsc --noEmit 0 errors; bun build clean; biome clean. A11Y-047–054 all fixed (scope="col" on all th, sr-only caption, aria-readonly on locked cells, skeleton role="status", AlertDialog manual-aria-labelledby removed, min-h-[44px] touch target, secondary badge text-edu-text-primary, #-header sr-only). Design-review: PASS (tokens-only, correct typography/spacing/radius/shadows/score-colors). Route: /teacher/grades. Mock-first (grade entry BE US-060 planned; assessment-scheme + gradePublishMode REAL). |
| US-E09.1 | Discipline Screen (teacher/principal): 3-tab Violations+Conduct+Leave; severity points (low -1/medium -3/high -5, floor 0); conduct auto-calc + manual override; leave approve/reject-with-reason; mock-first core (USE_MOCK → MockDisciplineRepository) | yes | yes | yes | yes | implemented | Unit: 19 domain tests (CalculateConductPoints 7 cases incl band boundaries + floor-at-0; RecordViolation valid/missing-student/missing-description/invalid-severity; ApproveLeave success + already-processed; RejectLeave success/min-10-char reason/already-processed; OverrideConductGrade valid/invalid-grade/missing-note). Integration: 9 DisciplineRepository tests (Mock CRUD: getViolations, recordViolation prepend, approveLeave, rejectLeave-with-reason, already-processed guard, overrideConductGrade; real-repo error mapping NETWORK_ERROR + 409→already-processed). E2E/Story: 9 Storybook stories with play() (Loading skeleton, ViolationsTab_Teacher[record CTA + Nặng badge], ViolationsTab_Principal[no CTA], ViolationsTab_Empty, ConductTab[override], LeaveTab_WithPending, LeaveTab_Reject[10-char dialog validation], LeaveTab_Empty, ErrorState[retry]). 425/425 Vitest pass (86 files); tsc --noEmit clean; bun lint clean (1 pre-existing admin-roster warning untouched); bun run build green. Routes: /teacher/discipline, /principal/discipline. Tokens: edu-error-dark (ADR 0040) for high severity. |
| US-E09.2 | Student Conduct Screen (student + parent): conduct summary card (points + grade badge); own violations list (read-only); leave request form (Sheet + RHF+zod, reason ≥10 chars, date ≥ today); leave history with status badges + rejection reason; parent view shows child name+class; mock-first core | yes | yes | yes | yes | implemented | Unit: submit-leave-request.use-case (5 cases: valid, equal-today, past-date→invalid-date, short-reason→reason-too-short, trimmed-reason); get-my-conduct-summary.use-case (delegate). Integration: 5 MockDisciplineRepository cases (getMyConductSummary own/fallback, getMyViolations filter, getMyLeaveRequests filter, submitLeaveRequest prepends pending). E2E/Story: 7 Storybook stories with play() (Loading skeleton, StudentWithViolations [grade badge+violations+status badges], StudentEmptyViolations [empty states], LeaveHistoryWithRejection [rejection reason], ParentView [child info], LeaveRequestForm [label linkage+10-char validation], ErrorState [retry]). 436/436 Vitest pass (88 files); tsc --noEmit clean; bun run build green. Routes: /student/conduct, /parent/conduct. A11Y: A11Y-001–007 all fixed (contrast→edu-text-secondary, form error focus, role="alert" removed from static list, aria-live loading, touch target min-h-11, Sheet close i18n, landmark h2+progressbar label). Design-review: 19/20 PASS. |
| US-E09.3 | Staff Leave Management (admin): standalone /admin/staff-leave; list staff leave requests; status filter pills (all/pending/approved/rejected) + start-date range; approve/reject with inline reason editor (reason ≥10 chars); 3 stat cards (pending/approved-this-month/total-days); toast confirm; mock-first core | yes | yes | yes | yes | implemented | Unit: 11 use-case tests — GetStaffLeaveRequestsUseCase (3: success, status forward, error propagate), ApproveStaffLeaveUseCase (3: ok, not-found, already-processed), RejectStaffLeaveUseCase (5: missing-reason, too-short, valid+trim, not-found, already-processed). Integration: MockStaffLeaveRepository (8-request seed, mutable approve/reject with not-found + already-processed guards). E2E/Story: 7 Storybook stories with play() (Loading skeleton, EmptyState, AllRequests, FilteredPending [aria-pressed pill filter], ApproveFlow [status flip + toast], RejectFlow [reason textarea + confirm + toast], ErrorState [banner + retry]). 448/448 Vitest pass (91 files); tsc --noEmit clean; bun run build green; biome clean. A11Y: aria-pressed filter pills in labelled fieldset/legend, reject textarea label+aria-required+aria-describedby hint, useEffect focus on editor open, toast aria-live=polite, icon-only-free action buttons with aria-label, decorative avatar aria-hidden, min-h-11 touch targets, tokens-only colors. Admin-only via existing admin/layout.tsx guard. |
| US-E10.1 | Messaging: 2-pane inbox (Direct+Groups tabs); chat bubbles; date dividers; system messages; typing dots (isTyping-gated); SSE mock realtime (useEffect, ADR 0041); new conversation modal; mock-first social service (ADR 0042) | yes | yes | yes | yes | implemented | Unit: send-message.use-case (3: ok/empty-body/max-length), create-conversation.use-case (2: ok/empty-recipients). Integration: messaging.mapper (3: toConversation/toMessage/toContact), MockMessagingRepository (3: getConversations/getMessages cursor-paged/sendMessage persisted/createConversation). E2E/Story: 9 Storybook stories with play() (DirectTabPopulated, GroupTabPopulated, ChatWindowOpen[role=log+date-dividers+system-msg], SendMessage_Optimistic[optimistic bubble+input-clear], NewConversationModal_Open[Radix portal], EmptyState[CTA], Loading[smoke], LoadError[alert], MobileView[375px]); ConversationList stories (Loading skeleton, DirectTabPopulated, ErrorState). 464/464 Vitest pass (95 files); tsc --noEmit clean; bun build green; biome clean. A11Y: tablist aria-controls+tabpanel, online dot sr-only, unread badge aria-hidden, role=log aria-live=polite, input label htmlFor, focus-visible rings, TypingIndicator motion-safe+sr-only, mobile back-button focus mgmt, DialogDescription sr-only. Route: /messages (shared). ADR 0041 (SSE in presentation layer), ADR 0042 (contacts service deferred). |
| US-E10.2 | Notifications Center: type taxonomy (grade/attendance/discipline/announcement/system); tab filters; SSE realtime prepend + Sonner toast; mark-read/mark-all-read; cursor load-more; mock-first noti | yes | yes | yes | no | implemented | unit: use-cases (13) + mapper (7); integ: repository envelope/error (12) + realtime event.new parse/invalidation (7); story: 10 Storybook states; mock-first (noti service not yet built, decision 0009) |
| US-E10.3 | Announcements (admin/principal): compose->preview->send pipeline; audience presets; priority; schedule/draft/send; read receipts; fan-out via SSE; mock-first noti | yes | yes | yes | no | implemented | unit: create-announcement (ok-now/ok-scheduled/ok-draft/title-too-short/body-too-short/no-audience/schedule-past-date×2), delete-announcement (ok/not-found), get-announcements, mapper (6 cases), toFailure (4). Integration: AnnouncementRepository (CRUD/remind/recipients/envelope). Story: 19 Storybook stories with play() (Loading/EmptyState/EmptyState_CTA/ListWithAllStatuses/CardFields_UrgentAndProgress/FilterPills_SentTab/CreateDrawer_Validation/CreateDrawer_CharCount/CreateDrawer_Send/CreateDrawer_SendSubmit/CreateDrawer_SaveDraft/CreateDrawer_ScheduleMode/CreateDrawer_PreviewToggle/DetailSheet_ReadReceipts/DetailSheet_RecipientFilter/DetailSheet_Remind/DeleteDialog/DeleteDialog_Confirm/A11y_DrawerAudienceGroupLabel). 558/558 Vitest pass (108 files); tsc --noEmit clean; bun build green; biome clean. A11Y-017–030 all fixed (contrast: bg-edu-error-dark on delete btn, text-edu-error-text on form errors, text-edu-text-primary on active pills, text-edu-text-secondary on char-count, bg-edu-success-text on progress fill; ARIA: progressbar role+attrs, urgent card aria-label, aria-invalid+aria-describedby on all form fields, audience fieldset aria-describedby+role=alert, Sheet closeLabel i18n, skeleton aria-busy/role=status). Design-review gate: PASS. AC coverage: 15/15. Route: /admin/announcements. Mock-first (noti service not yet built). |
| US-E11.1 | Student Exam: list+briefing+taking (timer+MCQ+navigator+flag)+results (score circle+rank+per-question review); auto-submit at 0; score color rules; mock-first lms | yes | yes | yes | no | implemented | unit: submit-exam.use-case (5) + calculate-score/scoreColorClass (10); integ: exam.mapper (3) + exam.mock.repository (6); story: 4 screens × states (ExamList Loading/AllStatuses/FilterTabs/Empty; Briefing gate; Taking AnswerFlow/Flag/SubmitModal + Timer Warning/Error; Result Pass/Fail/Review) — play fns authored (storybook vitest runner broken env-wide, see memory); routes /student/exams + /student/exams/[examId]; tsc+biome+build green; full suite 556 pass; mock-first (lms service not yet built, decision 0014) |
| US-E11.2 | Lesson Bank: grid/list + upload drawer (pdf/pptx/mp4/link) + visibility (private/dept/school) + detail sheet; principal read school-wide; mock-first lms | yes | yes | yes | no | implemented | unit: upload-lesson + delete-lesson use-cases, lesson-file-type; integ: lesson.mapper + lesson-bank.repository (envelope/error); story: 4 Storybook states (Populated/EmptyTeacher/NoMatch/PrincipalReadOnly); route /teacher/lesson-bank; tsc+biome+build green; mock-first (lms service not yet built, decision 0014) |
| US-E11.3 | Exam Bank + Builder: list (draft/published); full-screen 2-col MCQ builder; reorder; publish pipeline; admin aggregate read-only; mock-first lms | yes | yes | yes | no | implemented | unit: create/publish/delete use-cases; integration: exam-bank.repository.test.ts (mock + envelope); e2e: ExamBankScreen + ExamBuilderScreen stories w/ play(); 779 vitest pass, tsc clean, next build green; Up/Down reorder per ADR 0043 |
| US-E11.5 | Mixed Exam Result: submitted_pending_essay status; MCQ auto-score + essay pending-teacher-grade banner; essay textarea in ExamTaking; mixed briefing indicator; grade-book deep link on completion; mock-first lms | yes | yes | yes | no | implemented | unit: calculatePartialScore (5 cases), isResultFinal (2 cases), mapExamResult pending-essay branch (6 cases); integ: mock repo returns MOCK_PENDING_ESSAY_RESULT for exam-005; story: 5 Storybook stories (ExamList_PendingEssayCard, Briefing_MixedIndicator, Taking_EssayQuestion, Result_PendingEssay, Result_CompletedAfterEssay) all with play() assertions; ADR-0048 Accepted; a11y A11Y-001..006 fixed; DR-E11.5-001..003 fixed; 924/924 tests pass; tsc 0 errors; bun build green; mock-first (lms service not yet built, decision 0014) |
| US-E11.6 | Student Lesson Player: StudentCourses grid (All/InProgress/Completed tabs, hover lift); LessonPlayer 2-col (content 60% + chapter nav 40%); 3 content types (video controls/pdf embed+download/text scroll); Notes + Q&A tabs; progress tracking + mark-complete; mock-first lms | yes | yes | yes | yes | implemented | unit: calculate-course-progress (6 cases), mark-lesson-complete use-case (ok/idempotent/not-found/unknown), list-courses + get-course-lessons use-cases, lesson-player.derive (9 cases: initial-pick/active-vm/next/progress/patch), actions.test.ts (6 RBAC-rejection + happy-path cases for all 5 Server Actions); integ: lms.mapper (color→tone + hierarchy/done/empty-chapter), MockLmsRepository (seed tones, mark-complete persist+recompute, idempotent, note upsert, question prepend); e2e: 15 Storybook interaction stories (CoursesGrid_Loading/AllTab/InProgressTab/Empty; LessonPlayer_Video/Pdf/Text; ChapterList_Navigation; ChapterList_CollapseExpand; VideoPlayer_KeyboardControls; ChapterList_MobileToggle; LessonPlayer_EmptyContent; MarkComplete_Flow; Notes_Save; QA_Ask) all with play(); routes /student/courses + /student/courses/[courseId] with requireRole(["student"]) on pages + all 5 server actions; 1237/1237 vitest pass, tsc 0 errors, bun run build green, biome clean; ADR-0050 media-surface token for video faux-chrome; tech-lead review + a11y audit both Approved after 1 fix round (A11Y-001 tone-text contrast, A11Y-002 card accessible name + 4 minors); design-review gate APPROVED; QA Go/No-Go: PASS; mock-first (lms service not yet built, decision 0014) |
| US-E11.4 | Teaching Plan / PPCT: weekly grid (subject x class x term); inline edit; draft/submit/approve/reject workflow; principal review + rejection reason; mock-first core | yes | yes | yes | no | implemented | unit: submit (ok/not-draft/insufficient-cells), approve (ok/not-submitted), reject (ok/not-submitted/invalid-reason), get (found/null), saveCell (ok/not-draft); integ: MockTeachingPlanRepository (11 cases — submit/approve/reject/listPending/not-found); story: 11 stories (TeacherGrid_Loading/WithContent/EmptyState/RejectedBannerTeacherView/InlineEdit_Flow/SubmitFlow/Submitted; PrincipalReview_Populated/ApproveFlow/RejectFlow/Empty); routes /teacher/teaching-plan + /principal/teaching-plan; tsc 0 errors, biome 0 errors in teaching-plan, 618/618 tests pass (120 files), bun build clean; mock-first (core PPCT US-051 deferred) |
| US-E12.11 | Admin Settings: gradePublishMode toggle (SELF_PUBLISH / ADMIN_APPROVAL); switch warning dialog (ADMIN_APPROVAL→SELF_PUBLISH); shortcut links to /admin/calendar + /admin/assessment; BE REAL via SCHOOL_SETUP_EP.operationalSettings (US-059 live) | yes | yes | yes | yes | implemented | Unit: GetOperationalSettingsUseCase (2: ok/network-error), UpdateOperationalSettingsUseCase (3: ok/network-error/invalid-mode) = 5 use-case; Integration: AdminSettingsRepository (5: GET success+mapping, GET network-error, PUT success body assert, PUT SCHOOL_FORBIDDEN→forbidden, unknown code→unknown) = 5 repo; Story: 7 stories (SelfPublishActive/AdminApprovalActive/ReadOnly/Loading/SwitchToSelfPublishWarning/SaveSuccess/SaveError) with play() assertions; 628/628 Vitest pass (123 files); tsc --noEmit 0 errors; bun build green (/admin/settings in output); biome clean. A11Y-031–040 all fixed (Save btn bg-edu-primary-dark 4.56:1, Confirm btn bg-edu-error-text 5.44:1, dialog desc text-foreground 12.36:1, icon text-edu-warning-foreground, aria-live polite, aria-describedby radios, motion-reduce). Design-review: PASS. GradePublishMode/OperationalSettings imported from admin-school-setup (not redefined). |
| US-E12.12 | Audit Log: append-only read of grade/conduct/record mutations (GDPR Nghi dinh 13/2023); cursor-paginated; filter entity type+action+actor+date; mock-first (core US-064 not confirmed live) | yes | yes | yes | yes | implemented | unit: use-case (5) + mapper (8) + helpers (15) + failure retryability (3); integration: mock repo (9) + REAL repo contract-shape (13); e2e: Storybook interaction 15 tests across 5 files (Loading/List/Filter/LoadMore/LoadMore_Error/EmptyState/ErrorState); platform: tsc + bun build clean; REAL repo fully wired but DI selects Mock while USE_MOCK=true — flip needs BE US-064 confirmation first |
| US-E13.6 | Grade Book (multi-role read): column-banded TX/GK/CK headers; score colors (>=8 success <5 error); five-band rank distribution; student/parent single-row; gradePublishMode gate; mock-first | yes | yes | yes | yes | implemented | Unit: rank-band (6: boundary thresholds), rank-distribution (3: percentages+counts), grade-book.mapper (5: weighted avg reuse, conduct map, publish-mode thread), get-grade-book/get-my-grades/get-child-grades use-cases (3 each = 9 total) = 23 domain+mapper unit; Integration: MockGradeBookRepository (5: getGradeBook/getMyGrades/getChildGrades + error paths); Story: 15 Storybook stories — GradeBookTable (TeacherView_WithScores/PrincipalView/StudentView_SingleRow/ParentView_SingleRow/PublishGateBanner/EmptyState) + GradeBookScreen (Loading/TeacherView_WithScores/PrincipalView/StudentView_SingleRow/ParentView_SingleRow/PublishGateBanner/SelectionChange/EmptyState/ErrorState) with play() assertions covering all 12 ACs; 739/739 Vitest pass (146 files); tsc --noEmit 0 errors; bun build green (5 new routes: /teacher/grade-book + /principal/grade-book + /admin/grade-book + /student/grades + /parent/grades); biome clean. A11Y-061–069 all fixed (4×text-muted→text-edu-text-secondary contrast, duplicate CTA removed, aria-live+atomic on banners, sr-only null score, aria-hidden decorative bars). Tech-lead: APPROVED. Design-review: PASS (tokens-only, TX/GK/CK tints correct, edu-info banner, shadow-card present, no raw colors). Routes: /teacher/grade-book, /principal/grade-book, /admin/grade-book, /student/grades, /parent/grades. Mock-first (grade BE US-060 planned; assessmentScheme + gradePublishMode REAL). Shared component: components/shared/grade-book-table/ (canonical home for multi-role reuse). |
| US-E14.4 | Grade Approval pipeline: admin approves PENDING_APPROVAL->PUBLISHED->LOCKED batches; per-batch review SideSheet (mini grade table+distribution+stats); bulk-lock; request-revision-with-note (min 10 chars); gradePublishMode=ADMIN_APPROVAL gate; RBAC admin-only; mock-first core | yes | yes | yes | yes | implemented | Unit: ApproveGradeBatchUseCase (3: ok/not-pending/batch-locked), RequestGradeRevisionUseCase (3: ok/note-too-short/not-pending), BulkLockBatchesUseCase (3: ok/empty-no-op/not-published), gradeBandKey (11 boundary+null cases) = 20 use-case + mapper unit; Integration: GradeApprovalMockRepository (5: list/detail/approve/requestRevision/bulkLock + status guards); batch.mapper (mapBatch, mapBatchDetail distribution counts, null-average→null-gradeBandKey) = 7 mapper + 5 repo integration; Story: 9 Storybook stories with play() (Loading/ListMixedStatuses/PendingFilter/BatchReviewSheet/ApproveFlow/RevisionRequestFlow/BulkLockFlow/SelfPublishModeWarning/EmptyState) — interaction play fns authored; 711/711 Vitest pass (139 files); tsc --noEmit 0 errors; bun build clean. A11Y-055–060 all fixed (LOCKED badge contrast text-foreground, band-label contrast text-edu-text-secondary, both tables aria-label, Sheet closeLabel i18n, Info icon text-foreground, motion-safe: chart animation). MUST-1 (tech-lead): gradeLabel→stable GradeBandKey, translated at render. Design-review: PASS (tokens-only, score-color imported not duplicated, server-only infra, no raw colors). Route: /admin/grades/approval. Mock-first (grade-approval BE US-060 planned). E14.2 tests untouched and green. |
| US-E14.5 | Academic Record Viewer / Hoc ba: multi-year timeline; subject grid (HK1/HK2/Ca nam/Xep loai/Hanh kiem); seal indicator; all roles; student/parent single-row; mock-first core | yes | yes | yes | no | implemented | unit: record assembly + score-tier/conduct mapping + sealed-indicator; integ: mapper + mock repo (multi-year, sealed+unsealed); story: multi-year/sealed/per-role; 4 RBAC routes; seal ACTION deferred to E14.6; tsc+biome+build green; mock-first (core US-064 planned) |
| US-E14.6 | Academic Record Seal: allLocked gate; bulk-seal per class/term/year; two-ADMIN unseal confirmation; audit trail; HIGH-RISK data integrity; mock-first core | yes | yes | yes | yes | implemented | Unit: 16 use-case tests (seal ok/not-all-locked/already-sealed; initiate-unseal ok/20-char boundary/not-sealed; confirm-unseal ok/same-admin/no-pending/self-approve + defense-in-depth "self-approve rejected when adminCount>=2", added in revision pass after fe-tech-lead-reviewer found the gate bypassable). Integration: 12 mock-repo tests (seal+SEAL audit, unseal-initiate not-sealed guard, confirm+UNSEAL audit, self-approve fallback adminCount=1, pending filter). E2E: 14 Storybook interaction stories (Loading/AllLockedGate_OK/NotOK/SealConfirmDialog/SealSuccess/AuditTrail/AuditTrail_Empty/UnsealInitiate/UnsealConfirm/UnsealSameAdminError/UnsealSelfApproveFallback/UnsealOwnRequestMultiAdmin/PageError/SealTab_EmptyBatch/UnsealTab_EmptyPending) green via vitest.storybook (4 added by fe-qa-playwright gap-closing pass). RBAC: actions.test.ts 9/9 Server Actions (6 reads + 3 mutations) reject non-admin before use-case runs — 6 reads were originally ungated (tech-lead finding), fixed + fully test-proven. Platform: `tsc --noEmit` + `bun run build` clean; repo-wide `bun vitest run` 1093/1093. Mock-first (core seal US-064 planned); real HTTP repo scaffolded (throws not-implemented). ADR-0037 (+2026-07-05 amendment: 20-char reason, binding over original 10-char draft) governs two-ADMIN gate; gate independently re-verified server-side in ConfirmUnsealUseCase (defense-in-depth). Review: fe-tech-lead-reviewer Revision Required -> fixed -> Approved; fe-accessibility-auditor Fail-with-findings -> fixed -> Pass; fe-qa-playwright Go. Design-review gate: pass (Evidence in story packet). |
| US-E09.4 | Parent Discipline & Leave View: parent reads child's conduct summary (score + grade badge) + violation history (read-only) + files leave requests on child's behalf; multi-child switcher; mock-first core | yes | yes | yes | yes | implemented | Unit: 5 use-case suites — GetChildrenUseCase, GetChildConductSummaryUseCase, GetChildViolationsUseCase, GetChildLeaveRequestsUseCase, SubmitChildLeaveRequestUseCase (valid/equal-today/past-date→invalid-date/short-reason→reason-too-short/trimmed-reason) + conductColorVar domain rule (10 boundary tests: ≥90→success, ≥70→primary, ≥50→warning, <50→error). Integration: MockDisciplineRepository child-scoped (7 cases: getChildren, getChildConductSummary own/not-found, getChildViolations per-child, getChildLeaveRequests with-rejection, submitLeaveForChild prepends pending/not-found). E2E/Story: 11 Storybook stories with play() (Loading_Skeleton, SingleChild [no tablist+score 82+Khá+no-record-btn], MultiChild [tablist+2-tabs+aria-selected], ChildSwitch_FormReset [open form→switch→form-closes+c2-score-94], EmptyViolations [shield empty+no-record-btn], LeaveForm_Valid [submit+banner+optimistic-pending-row], LeaveForm_Validation [short-reason error+form-stays-open], LeaveForm_PastDate [past-date→'Ngày nghỉ phải từ hôm nay trở đi'+form-stays-open], LeaveHistoryWithRejection [3-status-badges+rejection-reason+no-cancel], ErrorState [error-banner+retry-btn], ReadOnlyEnforcement [Chỉ-xem-badge+no-edit/delete/withdraw]). 912/912 Vitest pass (185 files); tsc --noEmit clean; bun run build green. Routes: /parent/discipline. WCAG 2.1 AA: A11Y-E09.4-001–008 all fixed (tablist ARIA ids/controls/panel, arrow-key focus-only, text-edu-text-secondary contrast 5.10:1, text-edu-success-text ShieldCheck 5.24:1, min-h-11 Input+SelectTrigger, scoreAriaLabel i18n key). Design-review: Pass (DR-GATE-001/002 ConductCard readOnly badge + empty-state Inbox icon contrast fixed). Security: parentId/submittedBy absent from request bodies; RBAC guard fires before any fetch; 'Ghi nhận vi phạm' absent from DOM. |
| US-E13.7 | Grade Book Parent Child-Switcher (DR-002): ChildSwitcher tabs for parent with ≥2 children; switches GradeBookTable dataset per childId; single-child path unchanged; no regression on E13.6 | yes (15: get-child-list 3 + get-child-grades 3 + child-switcher keyboard 9) | yes (mock grade-book repo: getChildList + getChildGrades c1/c2/fallback) | yes (4 ChildSwitcher stories + 4 GradeBookScreen parent stories) | clean | implemented | mock-first core (INT-001 child-list + INT-002 child-grades); ChildSwitcher uses aria-disabled (not native disabled) — stories assert aria-disabled='true'; build green |
| US-E15.1 | Timetable Read-only View (Student + Parent): weekly grid Tiết 1–10 + Giải lao trưa; filled cells show subject/teacher/room (no edit affordance); empty cells show "—"; parent multi-child card picker; guard-first Server Actions (requireRole student/parent); mock-first core | yes | yes | yes | yes | implemented | domain: get-my-timetable/get-child-timetable/get-child-list use-cases (not-found/network-error/no-child paths) + mapper (subject-color resolution incl. Địa lý placeholder) = unit; MockWeeklyTimetableRepository (11A2 full/8B1 sparse/unknown→not-found) = integration; 7 Storybook interaction stories (FullWeek/Empty/SingleChild/MultiChildSwitch/Loading/Error/Mobile375) + 8 RBAC unit tests on both actions.ts (reject-before-DI-call) = e2e-equivalent; tsc clean, bun build green; tech-lead APPROVED, a11y PASS (2 Major contrast fixes applied — week-nav active-week button, warning subject-text token), QA CONDITIONAL PASS (Go); Địa lý subject-color token remains a documented placeholder pending a fast-follow ADR (≥0051) |
| US-E10.4 | Messaging Enhancements (DR-008): group lifecycle (create-group 2-step modal, group info panel, add/remove member, leave/delete admin-gated) + message interactions (context menu, reply/quote, pin, delete own ≤1h); extends US-E10.1; mock-first social | yes | yes | yes | yes | implemented | Unit: CreateGroupUseCase (4: ok/no-name/too-short/no-members), DeleteMessageUseCase (3: ok/not-own/expired), PinMessageUseCase (2: ok/not-admin), RemoveGroupMemberUseCase (3: ok/not-admin/other-admin), DeleteGroupUseCase (2: ok/not-admin), LeaveGroupUseCase + AddGroupMembersUseCase + UpdateGroupUseCase = 47 tests; Integration: MockMessagingRepository (group lifecycle, pin/unpin, deleteMessage, per-role seeding) + messaging.mapper; Story: 36+ Storybook stories covering all 12 ACs — CreateGroupModal (8: step1-empty/valid/validation-error/step2-no-members/step2-with-members/submit-loading/submit-error/mobile), GroupInfoPanel (8: open/admin/non-admin/delete-confirm/pinned-messages/member-offline/mobile + reply-strip), MessageContextMenu (4+: own-admin/own-non-admin/other/expired + keyboard-nav), ChatBubble (5: reply-own/reply-other/deleted/highlighted/loading-skeleton), MessagingScreen (9+: direct/group/chat-window/send/new-convo/empty/loading/error/mobile + optimistic-create + reply-strip); 835/835 Vitest pass (167 files); tsc --noEmit 0 errors; bun build green; biome clean. Domain admin gate proven: not-group-admin failure in removeGroupMember/deleteGroup/pinMessage use-cases. Design-review: PASS (tokens-only except 2 spec-mandated hex literals; 2 low-severity pre-existing findings, 0 blocking; motion-safe gates confirmed; focus trap Radix Dialog/Sheet; keyboard ARIA menu). QA: Go (AC-1 through AC-12 covered). |
| US-E10.5 | Messaging defect fixes: DEF-01 scrollToMessage timer leak on unmount; DEF-02 onAddMembers no-op stub wired to AddGroupMembersUseCase via addGroupMembersAction Server Action + AddMembersModal (TanStack mutation, onSuccess group cache update + conversationList memberCount sync, onSettled groupKey invalidation); DEF-03 screen-level scroll-to-pinned + highlight-flash Storybook story | yes | no | yes | yes | implemented | Unit: highlight-timer.test.ts (4 cases vi.useFakeTimers: applies+clears after 3000ms, rapid-click no leak, unmount cleanup prevents fire, custom duration). E2E/Story: AddMembersModal (5 stories: Default/SelectAndSubmit/Empty/Submitting/SubmitError), MessagingScreen AddMembers_Wired (wiring assertion), ScrollToPinned_Highlight_Wired (highlight applied). 839/839 Vitest pass (168 files); tsc --noEmit 0 errors; bun build green; pre-push gate green. No new tokens. |
| US-E15.2 | Teacher Schedule View (read-only lịch dạy cá nhân): weekly grid reusing TimetableGrid (cellVariant=teacher → class name in line 2); slots span multiple classes by teacherId; no edit affordance; mock-first core | yes | yes | yes | yes | implemented | domain: GetMyTeachingScheduleUseCase (ok/not-found/network-error, mirrors get-my-timetable) = unit; MockWeeklyTimetableRepository.getByTeacher (multi-class TEACHER_RAW fixture + not-found) + Server Action RBAC (requireRole teacher, guard-fail/pass/use-case-failure) = integration; 5 Storybook interaction stories (TeacherView_FullWeek incl. AC2 no-edit-affordance + AC3 subjectColor/15+border class assertions + AC7 sr-only caption / TeacherView_EmptySchedule / Loading_Skeleton / ErrorState / TeacherView_Mobile 375px) = e2e-equivalent; reused TimetableGrid cellVariant="teacher" (already scaffolded by US-E15.1) + ExportPdfButton/ReadOnlyField promoted to shared timetable-view/ modules (decision 0026 review fix); tsc clean, bun build green (242 files/1278 vitest + 12/12 storybook interaction); tech-lead APPROVED (post 1 revision round: verbatim ExportPdfButton duplication fixed), a11y AA PASS (0 blocking), QA Go (8/8 AC covered, 0 defects) |
| US-E17.1 | Responsive stat-card grid (UX-03, DR-010): replace repeat(4,1fr) with repeat(auto-fit,minmax(200px,1fr)) on discipline-screen (×3 grids), teacher-dashboard-home (update 180px→200px), principal-dashboard, student-dashboard, attendance-summary-card, exam-result (found via OQ-003 grep, beyond original 7-file list); breakpoints: >1024=4col, 640-1024=2col, <640=1col; Tailwind arbitrary value; no JS breakpoints; touch targets N/A (no interactive elements in any stat card, confirmed by a11y audit); WCAG 1.3.2 DOM order preserved | yes (6 responsive-stat-grid.test.ts files, 39 tests: assert target class present, forbidden old classes absent, gap-4 preserved/normalized, per grid) | n/a — pure CSS, no HTTP boundary | yes (Storybook Viewport375 interaction stories on attendance-screen/discipline-screen/exam-result/teacher-dashboard-home; QA closed 2 gaps: exam-result vacuous fixture assertion, discipline Conduct-tab zero coverage) | implemented | DR-010; story packet at docs/stories/epics/E17-ux-polish/US-E17.1-responsive-stat-grid/; tech-lead Approved; a11y PASS no findings; QA Go (2 test-coverage fixes, no production bugs); 989/989 suite green; tsc/build clean; principal-dashboard/student-dashboard are RSC with no story file (non-blocking, covered by unit test) |
| US-E17.2 | Grade table mobile scroll + sticky column (UX-03, DR-010): add -webkit-overflow-scrolling:touch + min-w-[640px] on table + border-r border-border on sticky first column cells to components/shared/grade-book-table/grade-book-table.tsx; existing overflow-x-auto + sticky left-0 preserved; added role=region + aria-labelledby (via useId, points at existing table caption, zero new i18n keys) + tabIndex=0 for keyboard scroll (A11Y-001 fix) | yes (4: role+aria-labelledby resolves to tableCaption, webkit-overflow-scrolling inline style, min-w-[640px] on table, border-r+border-border+z-[1] on both sticky cells) | n/a — pure CSS/a11y, no HTTP boundary | yes (11 Storybook interaction stories, real Chromium via vitest.storybook.mts: MobileScroll_320/375, DesktopView_1280, TeacherView_Tablet768, ParentView_Mobile375 + existing role stories) | implemented | DR-010; story packet at docs/stories/epics/E17-ux-polish/US-E17.2-grade-table-mobile/; a11y audit found+fixed A11Y-001 (blocking, keyboard tabIndex) + A11Y-002 (should-fix, aria-labelledby replacing duplicate aria-label); tech-lead Approved; QA Go; 928/928 suite green; tsc/build clean; blocks US-E17.5, cross-ref US-E17.11 (same file, non-overlapping) |
| US-E17.3 | Messaging single-pane mobile toggle animation (UX-03, DR-010): mobile panes now always-mounted + absolutely stacked (transform-driven slide, 0.25s ease, motion-reduce:transition-none) instead of display:none, so the transition actually animates; off-screen pane gets aria-hidden + native inert (matchMedia-gated via useIsMobile, since neither attribute is CSS-media-toggleable); back button grown to >=44x44 (BACK_BUTTON_CLASS); conversation rows already >=44px; back button aria-label reuses existing messaging.chat.backToList key, zero new i18n keys/tokens | yes (pane-visibility.test.ts: listPaneClass/chatPaneClass transform+motion-reduce classes, paneAriaHidden, paneInert, BACK_BUTTON_CLASS — 69/69 messaging tests, 942/942 full suite) | n/a — pure UI/CSS/aria, no HTTP boundary | yes (11 Storybook interaction stories, real Chromium via vitest.storybook.mts: MobileSinglePane_Toggle, MobileReducedMotion_Correctness, SlideTransition_ComputedStyle, DesktopUnchanged_1280, MobileEmptyState_ChatPaneHidden, MobileLoadError_ChatPaneHidden, NoOverflow_320_Loaded/Empty/Error — 21/23 pass, 2 pre-existing unrelated E10.4 flakes) | implemented | DR-010; story packet at docs/stories/epics/E17-ux-polish/US-E17.3-messaging-mobile-pane/; a11y audit PASS (A11Y-001 pre-hydration matchMedia gap fixed, A11Y-002 Tab-walk hardening left optional/non-blocking); tech-lead Approved; QA Go (19/23 spec AC automated, 100% of story.md's critical AC subset); DEF-01 (chat-pane error UI missing, out-of-scope base-feature gap) logged as follow-up; blocks US-E17.7; tsc/build clean |
| US-E17.4 | Empty states — discipline violations + conduct + leave requests (UX-01, DR-010): upgrade violations-tab (remove green Check icon → ShieldOff+role=status), conduct-tab (ClipboardList), leave-tab teacher+parent-discipline (CalendarOff); canonical pattern — icon 64px `text-edu-text-secondary` aria-hidden (A11Y-001 fix: `text-edu-text-muted` was 2.95:1, below ≥3:1 floor), title 16px/700 primary, no CTA, role=status; all i18n keys existing; new shared `src/components/shared/empty-state/` extracted (decision 0026, resolves US-E17.5 tech-debt flag) | yes (empty-state.test.tsx: 7 tests, renderToStaticMarkup DOM proof — role=status, aria-hidden, no button/h2/h3, conditional body/cta, className merge; node env, no RTL/jsdom in repo) | n/a — pure client-side conditional, no HTTP boundary | yes (Storybook interaction, real Chromium: empty-state.stories.tsx 3/3 Default/WithBody/WithCta; discipline-screen.stories.tsx ViolationsTab_Empty/ConductTab_Empty/LeaveTab_Empty strengthened+added, blocked from execution by pre-existing env-wide useRouter-mount issue affecting 17 files repo-wide, not this diff; ParentDisciplineScreen_EmptyLeaveRequests 11/12 pass, 1 pre-existing unrelated failure confirmed via main diff) | implemented | DR-010; story packet at docs/stories/epics/E17-ux-polish/US-E17.4-empty-states-discipline/; tech-lead Approved; a11y audit found+fixed A11Y-001 (Major, icon contrast) — re-confirmed PASS; QA Go, 100% AC coverage (40/40), closed 2 pre-existing test-coverage gaps (ConductTab/parent LeaveHistorySection had no empty-branch test); 949/949 unit + tsc/build clean; follow-ups logged out of scope: ViolationsList.tsx (parent) + leave-history-list.tsx (student-conduct) still carry old anti-pattern |
| US-E17.5 | Empty state — grade book table (UX-01, DR-010): upgrade GradeBookScreen local EmptyState to canonical pattern (FileText icon 64px muted, role=status, aria-live=polite, title=gradeBook.emptyState) for `vm.gradeBook===null` branch only (no-selection prompt unchanged, legacy dashed-border kept); shared GradeBookTable sticky-column border/bg pinned to explicit border-edu-border/bg-edu-card tokens (dark-mode correctness); note: design-spec.jsonc has typo grades.gradeBook.emptyState → actual key is gradeBook.emptyState | yes (grade-book-table.test.tsx DOM-attribute assertions for sticky-cell tokens) | n/a — pure client-side conditional, no HTTP boundary | yes (Storybook interaction stories, real Chromium via vitest.storybook.mts: EmptyState + NoSelectionUnchanged in grade-book-screen.stories.tsx; existing MobileScroll_320/375 in grade-book-table.stories.tsx re-verified green, untouched) | implemented | DR-010; story packet at docs/stories/epics/E17-ux-polish/US-E17.5-empty-state-gradebook/; tech-lead Approved (non-blocking debt flagged: extract shared components/shared/empty-state/ across screens, out of scope); a11y audit PASS 0 findings; QA Go; 942/942 unit + 26/26 storybook green; tsc/build clean |
| US-E17.6 | Empty states — notifications all/unread tabs (UX-01, DR-010): migrated notifications-center local EmptyState onto the canonical shared `components/shared/empty-state/` (BellOff/CheckCircle2 icon 64px, role=status, aria-hidden) — fixed the shared component's own body-text contrast bug (`text-muted-foreground`→`text-edu-text-secondary`, WCAG 1.4.3: 2.95:1 FAIL → 5.1:1 PASS), benefiting every consumer; sibling low-risk migration of `leave-history-list.tsx` (student-conduct) onto the same shared component; `ViolationsList.tsx` (parent-discipline) deliberately left unmigrated — its success-toned green empty state is semantically distinct (A11Y-E09.4-005); all i18n keys existing, no new tokens | yes (empty-state.test.tsx: 8 tests incl. red→green body-contrast assertion) | n/a — pure client-side conditional, no HTTP boundary | yes (Storybook interaction, real Chromium: notifications-center.stories.tsx EmptyAll/EmptyUnread strengthened with role=status/aria-hidden/body-class assertions + Loading/ErrorState/AllLoaded hardened to assert empty-state copy is suppressed; student-conduct-screen.stories.tsx re-verified green) | implemented | DR-010; story packet at docs/stories/epics/E17-ux-polish/US-E17.6-empty-states-notifications/; tech-lead Approved (2 non-blocking follow-ups: pre-existing WCAG 1.4.11 fail in unrelated `my-violations-list.tsx`, and consider optional tone/iconClassName prop on shared EmptyState if a 3rd success-toned variant appears); a11y audit PASS 0 findings; QA Go, 100% AC coverage, closed a suppression-assertion gap; 950/950 unit + tsc/build clean |
| US-E17.7 | Empty states — lesson bank + messaging conversations (UX-01, DR-010): migrated `LessonBankEmpty` (BookOpen/Search icon by hasActiveFilter, CTA gated by canUpload&&!hasActiveFilter) and `EmptyMessagingState` (MessageSquare icon, CTA gated by onStart) onto the canonical shared `components/shared/empty-state/` instead of the BA spec's literal inline-JSX upgrade — one-component-one-home (decision 0026), same precedent as US-E17.6; icon/body use `text-edu-text-secondary` (5.1–5.48:1), not spec's literal `text-edu-text-muted` (2.95:1 FAIL); CTA 44px inherited from shared `Button` `min-h-11`; `empty-messaging-state.tsx` passes `className="flex-1 justify-center"` for the messaging pane's flex-row fill/center; all i18n keys existing, no new tokens | n/a — no domain logic | n/a — pure presentation, no HTTP boundary | yes (Storybook interaction, real Chromium: `lesson-bank-empty.stories.tsx` AllVariant/FilterVariant/WithUpload/PrincipalNoUpload, `empty-messaging-state.stories.tsx` Default/WithCTA — 9/9 pass; each asserts role=status, icon aria-hidden, body text-edu-text-secondary class, CTA clientHeight>=44, CTA click fires callback, CTA absence per canUpload/hasActiveFilter/onStart) | implemented | DR-010; story packet at docs/stories/epics/E17-ux-polish/US-E17.7-empty-states-lessonbank-messaging/; tech-lead Approved (2 non-blocking CONSIDER notes); a11y audit PASS 0 findings; QA Go, closed AC-07.1 (Principal CTA-suppression) coverage gap with PrincipalNoUpload story; 48/48 AC coverage; 950/950 unit + tsc/build clean |
| US-E17.8 | DestructiveConfirmDialog shared component (UX-02, DR-011): canonical component at components/shared/destructive-confirm-dialog/ on shadcn AlertDialog; consolidates 7 existing feature-local dialogs (exam-bank delete/publish, grade-approval approve/bulk-lock, admin-roster unenroll, admin-settings switch, class-archive) + wires 2 net-new instances (announcements send-to-school, discipline violations delete — net-new mock-first delete-violation flow); focus-on-open + aria-busy + alertdialog role; Escape=cancel; discipline confirmLabel key corrected to `.confirm` (spec typo); staff-leave reject deliberately NOT migrated (bespoke mandatory reason-capture panel, documented deviation) | yes | no | yes | no | implemented | DEF-001 (announcements confirm dialog stuck open) found by QA, fixed; a11y round-1 3 Blocking + 2 Major fixed; story packet at docs/stories/epics/E17-ux-polish/US-E17.8-destructive-confirm-dialog/ |
| US-E17.9 | DetailPanelHeader shared component (UX-04, DR-011): canonical component at components/shared/detail-panel-header/; 3-zone row (back ghost btn left | title center | actions right); back button min-h-[44px] min-w-[44px] with aria-label=backLabel (untruncated); title truncates at 375px; actions collapse to icon-only below md via `sr-only md:not-sr-only` (corrected from spec's literal `md:hidden`, which was backwards); wired to announcements detail drawer (net-new key `announcements.backToList` — spec wrongly claimed pre-existing), messaging group-info panel (reused `messaging.chat.backToList`), exam-builder header (reused `examBank.builder.backAriaLabel`, replacing manual ArrowLeft implementation in builder-action-bar.tsx) | yes (detail-panel-header.test.tsx: 6 tests — aria-label untruncated, 44×44 classes, ghost variant + icon order, 3-zone no-crash, title typography, actions slot) | n/a — pure presentational, no HTTP boundary | yes (Storybook interaction, real Chromium via vitest.storybook.mts: Default/WithTitle/WithActions/WithTitleAndActions + MobileViewport with real 375px `page.viewport()` resize proving title truncation + icon-only action collapse; consumer-level back-button stories added for all 3 wirings — DetailSheet_BackButton, GroupInfoPanel_BackButton, BuilderActionBar Default/Saving/NotPublishable/MobileViewport; 29/29 this story's Storybook tests pass) | implemented | DR-011; story packet at docs/stories/epics/E17-ux-polish/US-E17.9-detail-panel-header/; tech-lead Approved; a11y audit found+fixed A11Y-001 (Critical, back-button contrast text-muted-foreground 2.95:1→text-edu-text-secondary 5.48:1) + A11Y-002 (Major, same fix on messaging edit-icon button); QA Go, 8/8 named AC covered (100%), closed 4 coverage gaps (no consumer-level back-button test previously existed for any of the 3 wirings; shared component's own MobileViewport story asserted nothing viewport-dependent); QA also flagged repo-wide INFO: `@storybook/addon-viewport` not installed so `globals.viewport` story params are inert under the vitest browser runner — worked around here via `page.viewport()`, other existing "MobileViewport" stories may want the same treatment (follow-up, not a US-E17.9 blocker); 1006/1006 unit + tsc/build clean; zero regressions confirmed via worktree diff against main |
| US-E17.10 | Loading skeletons: StatCard + TableRow (UX-05, DR-011): shared StatCardSkeleton/StatCardSkeletonGrid (52×52px icon box, h-7 w-16 value, h-3 w-20 label, 20px padding, 12px radius) in discipline-screen (count=4) + teacher `loading.tsx` (count=6) + student `loading.tsx` (count=4, RSC Suspense fallback for the 2 pure-RSC dashboards); feature-local TableRowSkeleton (min-h-[44px], border-b, varying widths) in discipline loading block; single role=status aria-busy wrapper + sr-only t('Common.skeleton.loadingAriaLabel') (A11Y-001 duplicate-region fix applied); motion-safe:animate-pulse inherited from shared Skeleton primitive | yes | no | yes | no | implemented | 1030 vitest passing (stat-card-skeleton.test.ts, table-row-skeleton.test.ts, teacher/student loading.test.ts, responsive-stat-grid.test.ts); tsc clean; bun build clean; Storybook Shared/StatCardSkeleton 3/3 browser-run green; tech-lead Approved; a11y PASS (A11Y-001 fixed, A11Y-002 deferred — systemic Skeleton primitive bg-accent contrast, ADR follow-up); QA Go; design-review pass; story packet docs/stories/epics/E17-ux-polish/US-E17.10-loading-skeletons/ |
| US-E17.11 | Touch target ≥44px: grade rows + violation rows (UX-08, DR-011): add min-h-[44px] to grade-book-table.tsx GradeRow data cells (th[scope=row] + 3 td); discipline violation rows (violations-tab.tsx) + student-conduct/parent-discipline read-only lists + icon-only delete button already ≥44px pre-existing (US-E17.4/E17.5/E17.8/E17.10) — verified by inspection + closed with regression test, no production markup change; sticky first column tokens (bg-edu-card/z-[1], US-E17.5) deliberately kept, not reverted to spec's literal bg-card/z-10 (AC-15 no-regression) | yes (grade-book-table.test.tsx 2 new tests min-h-[44px] tbody-only; my-violations-list.test.tsx + ViolationsList.test.tsx new py-4 regression tests) | n/a — pure CSS/layout, no HTTP boundary | yes (TouchTarget_Mobile375 story, real 375px page.viewport + live getBoundingClientRect ≥44px; ViolationsTab_Teacher story extended with row-height + icon-button 44×44 assertions) | implemented | DR-011; story packet at docs/stories/epics/E17-ux-polish/US-E17.11-touch-target-44px/; tech-lead Approved; a11y PASS (0 blocking, 1 non-blocking doc-drift note A11Y-011-01 py-2.5 spec vs py-3.5 code); QA CONDITIONAL PASS → gaps closed (AC-10/13 test coverage added, AC-15→AC-01 mislabel fixed, full 15-AC list reconciled in story.md); 1034/1034 vitest green (+2 baseline delta from prior E17.10 count), tsc/build clean; follow-up flagged (non-blocking, pre-existing from US-E17.5, not introduced here): dark-mode sticky-column `--edu-card`/`--edu-border` not overridden in `.dark` while `--card` is — candidate for a future dark-mode visual QA/token-reconciliation story |
| US-E17.12 | Contextual toast with entity/count/timestamp (UX-06, DR-011): upgrade announcements drawer to use sendToastContext ({recipientCount, time HH:mm}) when context available, fallback to sendToast; upgrade discipline violations record to use successContext ({studentName}) when context available, fallback to success; both new keys were already present in vi.json/en.json from a prior DR-011 batch (verified, not re-added); contextual 4000ms / generic 2000ms; generic keys never deleted; fallback branch unreachable via either screen's UI (recipientCount/studentName always populated at submit) so it's pinned by unit tests only | yes (send-toast-params.test.ts 6, violation-toast-params.test.ts 3 — contextual/generic/edge cases, deterministic Date injection) | n/a — pure presentational, no HTTP boundary | yes (Storybook: CreateDrawer_SendSubmit_ContextualToast passes; ViolationsTab_RecordViolation_ContextualToast correctly authored but blocked by a pre-existing repo-wide useRouter harness issue affecting the whole discipline-screen.stories.tsx file, confirmed unrelated via git stash against main) | implemented | DR-011; story packet at docs/stories/epics/E17-ux-polish/US-E17.12-contextual-toast/; tech-lead Approved; a11y PASS (0 blocking, A11Y-001 informational note re: 4000ms readability for long names, no code change needed); 1043/1043 vitest green, tsc/build clean |
| US-E17.13 | Setup stepper: progress bar + Bước N/M counter (UX-07, DR-011): school-setup-screen onboarding banner — replaced scaleX transform fill with inline `width:%` + `motion-safe:transition-[width] duration-[400ms] ease-out` (AC-08 no scaleX); added "BƯỚC N/M" counter via `stepper.progress {current,total}`; per-step status icons Check/Loader2/Circle (`stepper.stepComplete/stepCurrent/stepPending` aria-labels, none aria-hidden), Loader2 gated `motion-safe:animate-spin`; `aria-valuenow` now `Math.round()` integer via new domain `roundStepPercent()`/`currentStep` fields on `getSetupProgress()`; `aria-label` switched to `stepper.ariaLabel` (was `guide.progressAriaLabel`); feature-local only (FR-007, no shared component); all 5 stepper i18n keys pre-existing, zero net-new | yes (get-setup-progress.use-case.test.ts: 16 tests — 2/5=40, 1/3=33, 2/3=67, roundedPercent integer, currentStep 1-based incl. non-sequential + all-done cap + divide-by-zero guard) | n/a — pure client-side/domain calc, no HTTP boundary | yes (Storybook interaction, real Chromium: StepperZeroOfFive/StepperTwoOfFive in school-setup-screen.stories.tsx — aria-valuenow/valuemin/valuemax/aria-label, inline width (no scaleX), motion-safe:transition-[width] class present, counter text, per-status icon counts + color-token/motion-safe:animate-spin assertions added by QA) | implemented | DR-011; story packet at docs/stories/epics/E17-ux-polish/US-E17.13-setup-stepper/; tech-lead Approved; a11y audit found+fixed A11Y-001 (Minor, pending Circle icon text-muted-foreground 2.95:1→text-edu-text-secondary 5.48:1) + A11Y-002 (Minor, redundant SR "done" badge → aria-hidden); QA Go, 8/8 AC covered (AC-20 reduced-motion is accepted manual/platform-tier proof per story.md, not automatable in this harness); 1055/1055 vitest green, tsc/build clean; design-review gate pass (Evidence recorded in story.md) |
| US-E16.1 | Side-stripe ban: replace border-left on cards/rows/callouts/cells with bg-tint + 1px border (DR-009 impeccable fix) | yes | no | yes | yes | implemented | Unit: announcement-card.test.ts, conversation-item.test.ts, schedule-row-class.test.ts, parent-row-class.test.ts, staff-leave-tones.test.ts — assert tint classes present + no border-l-* on 8 affected sites; sidebar active-rail border-l-[3px] preserved (AC-10). 858/858 Vitest pass; tsc 0 errors; bun build green; DR-PASS; A11Y-Pass. |
| US-E16.2 | Error-ramp contrast: notification count badges white-on-error (2.36:1 FAIL) -> errorDark bg + errorForeground text (8.2:1 PASS) (DR-009 impeccable fix) | yes | no | yes | yes | implemented | Unit: header.test.ts — asserts NOTIFICATION_DOT_CLASS = bg-edu-error-dark, not bg-edu-error. Contrast #b91c1c on white = 8.2:1 (AAA). 858/858 Vitest pass; bun build green; DR-PASS. |
| US-E16.3 | a11y/interaction gaps: Card keyboard nav + focus ring, ProgressBar ARIA, icon-button aria-labels, dropdown outside-click + Escape dismiss, sidebar aria-current (DR-009 impeccable fix) | yes | no | yes | yes | implemented | Unit: card.stories.tsx Interactive/NonInteractive play() (role=button, Enter/Space → onClick×3; no button on static). grade-distribution-chart ARIA added. aria-labels i18n: userMenu, toggleNav. sidebar aria-expanded (was aria-pressed). 858/858 Vitest pass; tsc 0 errors; bun build green; DR-PASS; A11Y-Pass. |
| US-E16.4 | Layout-transition perf: ProgressBar fill width%->scaleX(GPU compositing); sidebar width->grid-template-columns transition (DR-009 impeccable fix) | yes | no | yes | yes | implemented | Unit: grade-distribution-chart.test.ts (fillTransform scaleX, clamped); sidebar-grid.test.ts (grid display, 260px/72px tracks). All 4 fills use scaleX+origin-left+motion-safe. Sidebar outer wrapper drives grid-template-columns. 858/858 Vitest pass; bun build green; DR-PASS. |
| US-E16.5 | Bounce easing: messaging typing indicator per-dot duration-desync -> staggered animation-delay (DR-009 impeccable fix) | yes | no | yes | yes | implemented | Unit: typing-indicator.test.ts — msg-typing-dot class, no animate-bounce, delays 0/0.18/0.36s; dots aria-hidden, sr-only text preserved. @keyframes msg-typing in globals.css gated by prefers-reduced-motion:reduce global reset. 858/858 Vitest pass; bun build green; DR-PASS; A11Y-Pass. |
| US-E18.0 | Gateway smoke + wiring playbook verify (school-config via Kong :8000, real `make stack-up`): envelope unwrap, error-code map, refresh rotation all confirmed against real BE, not mocks — no UI, validation-only story that seeds Wave 1-4 of epic E18 | yes (1 new: SCHOOL_INVALID_TENANT_ID→forbidden) | yes (real gateway round trip via web's own createHttpClient — not committed as a test file, ephemeral per tiny-lane scope, logged in story Evidence) | no | yes | implemented | docs/stories/epics/E18-be-wiring/US-E18.0-gateway-smoke/story.md; fixed SchoolConfigRepository SCHOOL_INVALID_TENANT_ID mapping + wired ensureFreshSession() into admin-school-setup.di.ts (decision 0018 proactive refresh, previously never called by any feature DI factory); found 2 cross-repo BE gaps (refresh reuse-detection not enforced; no local SUPER_ADMIN seed for make stack-up) logged in EPIC-OVERVIEW.md; 1279/1279 vitest, tsc clean, bun build green (117 routes) |
| US-E19.1 | Social Feed (school + class post/comment/reaction/pin, all roles, DR-012): net-new `src/features/feed/` (domain/infra/DI/presentation) + `(app)/(shared)/feed` route | yes | yes | yes | no | implemented | docs/stories/epics/E19-social/US-E19.1-social-feed/{requirements,integration,use-cases,story,spec,plan,component-architecture,state-design}.md; normal lane; consumes US-E19.2's shared `ReportContentDialog`/`DestructiveConfirmDialog` + `makeSubmitReportUseCase`/`makeRemoveContentUseCase` (no second impl). Unit: feed domain (canPost/menu-visibility/sort-posts policies table-driven, use-cases create/react/add-comment/toggle-pin-mock, failure retryable-map) + feed mapper (defaults/initials/reaction-normalize/role-map) + repo `toFeedFailure` code-only branching (scope-404 vs post-404 vs forbidden vs 422) + feed `actions.test.ts` (11 tests — Server Action wrappers over the shared moderation use-cases, incl. ADR-0052 no-reportId direct-removal path). Integration: `FeedRepository` envelope-unwrap + meta.pagination + reaction 404→post-not-found + togglePinMock zero-HTTP spy. E2E: 41 Storybook interaction tests (loading 3-skeleton, empty±CTA per role×scope incl. class-scope student/parent legs, error retryable+forbidden-no-retry, populated pinned-first+icon-label, composer full lifecycle incl. validation/forbidden/transient, scope switch + keyboard tablist nav + listbox-first multi-class activation, reaction add/replace/remove/rollback/404-concurrent-removal, comment full lifecycle incl. validation/transient/post-gone-collapse, menu role×author matrix incl. own-post-trigger-absent + Escape-focus-return, report-invokes-shared-dialog for BOTH post and comment kinds + stays-visible, remove-confirmed-refetches-and-drops (ADR-0052 no-reportId proof), pin toggle-zero-fetch+resort+unpin+non-persistence-across-refetch, pagination append, end-of-feed marker) + 4 (`LoadMoreButton`, promoted+shared). `fe-qa-playwright` Go verdict: 42/44 AC directly test-backed, 2 documented MINOR gaps (comment-thread loading skeleton, exact load-more→end-marker transition moment) — not blocking. ADR 0052: `RemoveContentRepoInput.reportId` optional (feed direct-removal, no report in scope) — sanctioned moderation edit + test. Promoted `LoadMoreButton` moderation→`components/shared/` (label props). `bun vitest run` feed+moderation+actions 105/105; `bun vitest:storybook` feed 41 + load-more 4 + moderation 22 (all green; 71 pre-existing failures across 17 unrelated feature files confirmed unrelated); `tsc`/`lint`/`build` clean, `/[locale]/t/[tenant]/feed` route compiles. Deviations documented in story Evidence (Result-repo convention, local-scope-state, comment-menu report-only). tech-lead: Approved. a11y: PASS after fix (A11Y-001 touch-targets on 6 controls raised to 44×44, A11Y-002 listbox-first multi-class activation — both fixed + re-verified green). Design-review gate: PASS (tokens-only, no anti-patterns, all 4 states present, 320px responsive OK). Platform (manual keyboard-only human pass) not separately executed — reasoned via Storybook keyboard-interaction stories instead, flagged honestly, not release-blocking. |
| US-E19.2 | Content Moderation (report queue + audit log, principal-only destructive remove, DR-013): shared ReportContentDialog + moderation feature (domain/infra/DI/presentation) + /principal/moderation route | yes | yes | yes | no | implemented | docs/stories/epics/E19-social/US-E19.2-content-moderation/{requirements,integration,use-cases,story,spec,plan,component-architecture,state-design}.md; high-risk lane; owns shared `moderation.reportDialog.*` contract; FR-108 role-gate = principal only (decision 0022 applied). Unit: use-cases + `toFailure` code-vs-misleading-message divergence + mapper + mock-repo (dismiss/remove→audit, deterministic MOCK_FORBIDDEN_REPORT_ID) + filter/format helpers. Integration: repo↔HTTP `toFailure` code-only branching; mock-repo audit-entry write-then-read (NFR-101 release-gate proof). E2E: 44 Storybook interaction tests (report-content-dialog 11 incl. focus-trap; destructive-confirm-dialog errorSlot forbidden/transient; ModerationScreen incl. never-optimistic remove, 403-distinct-no-retry, empty-positive-vs-filtered never conflated, non-principal-hides-remove, dismiss/remove happy/cancel/conflict/transient, audit-forbidden, duplicate-none, 320/375 responsive) — fe-qa-playwright Go verdict, AC coverage 50/51 in-scope (1 N/A, owned by US-E19.1). Fixed post-QA: dismiss now also invalidates `moderationKeys.audits()` (parity with remove). tech-lead: Approved; a11y: 0 blocker/critical/major (2 minor fixed). Platform (manual keyboard-only click-through pass, as opposed to automated keyboard-assertion tests which ARE done) still pending a human pass — flagged, not release-blocking. |
| US-E20.1 | Admin Parent-Student Link Management incl. destructive Unlink (DR-014 part 1): spec-only BA pass — no code yet | no | no | no | no | planned | docs/stories/epics/E20-parent-student-links/US-E20.1-admin-parent-links/{requirements,integration,use-cases,story,spec}.md; escalated normal→high-risk (Unlink = authorization hard gate); actor role = admin (decision 0022); core service mock-first |
| US-E20.2 | Parent Notification Consent Section, extends implemented Profile screen (DR-014 part 2): spec-only BA pass — no code yet | no | no | no | no | planned | docs/stories/epics/E20-parent-student-links/US-E20.2-parent-consent-section/{requirements,integration,use-cases,story,spec}.md; mounts in Profile identity column next to AccountRequestsCard; own `parentLinks.consentSection.*` i18n namespace |
| US-E21.1 | Admin Tenant Invitation Management (DR-015 part 1): spec-only BA pass — no code yet | no | no | no | no | planned | docs/stories/epics/E21-tenant-invitations/US-E21.1-admin-invitations/{requirements,integration,use-cases,story,spec}.md; actor role = admin (decision 0022); invite-role vocabulary maps to 5 system roles, "manager" = display alias for principal |
| US-E21.2 | Public Invitation Accept Onboarding, unauthenticated account creation (DR-015 part 2): spec-only BA pass — no code yet | no | no | no | no | planned | docs/stories/epics/E21-tenant-invitations/US-E21.2-invite-accept/{requirements,integration,use-cases,story,spec}.md; high-risk lane; binding security contract in decision 0051 (no client-supplied role/tenantId, ever) |
| US-E22.1 | Email Verification: banner + OTP dialog + Profile row, extends implemented Profile screen (DR-016) — full TDD implementation, all 3 iam endpoints real | yes | yes | yes (Storybook interaction, no Playwright harness in repo) | yes | implemented | docs/stories/epics/E22-email-verification/US-E22.1-email-verification/{requirements,integration,use-cases,story,spec,plan}.md; `bun vitest run` 1393 tests pass; repo contract tests for send/confirm incl. 3-way error mapping; `OtpInput` promoted to `components/shared/otp-input/`; design-review gate pass (impeccable detect.mjs 0 findings); tech-lead Approved + a11y Critical/Major fixed + QA 2 defects fixed (see story.md Evidence) |
| US-E10.6 | Messaging Presence Indicator, extends implemented Messaging (DR-017/US-E10.1/US-E10.4): 3-state presence (online/recent/offline) additive to `isOnline` boolean on ContactEntity/ConversationEntity/GroupMember; shared `PresenceDot` (components/shared) at conversation-list avatar, DM chat-header dot+caption, group member-panel dot+online-first sort+"N online" count; INT-401 `GET /noti/api/v1/presence` mock-first (own `IPresenceRepository`, not bolted onto `IMessagingRepository`); `presence.changed` SSE event additive to `RealtimeEvent` union + `queryKeysFor()` | yes | yes | yes | no | implemented | Unit: presence.ts derivation/rank/countable/sort (8), presence-caption.ts (6), get-presence.use-case (3), presence-gating.ts (5), presence-dot no-motion (7). Integration: presence.mock.repository (7), mapper additions incl. new group.mapper.test.ts (3+3), event.ts presence.changed accept/reject/tenant-drop (6), presence-invalidation.ts real-QueryClient harness (2). E2E/Story: presence-dot.stories (5), conversation-list presence variants (+5-6), new chat-window.stories.tsx (6), group-info-panel presence variants (+3) — 38 AC traced, 34/38 solid first pass, DEF-1 (AC-10.6.3.2 group-header-fetch violation) + no-motion gap both fixed and re-verified. Regression guard (FR-005): 14 existing suites (10 use-case tests, conversation-item, typing-indicator, pane-visibility, highlight-timer, messaging.mapper, messaging.mock.repository) independently diff-verified zero assertion changes vs main by both reviewer and QA. 277/277 files, 1552/1552 Vitest pass; tsc --noEmit clean; bun build green. A11Y: A11Y-001 (blocking, sr-only presence text unreachable inside aria-labelled row button) + A11Y-002 (major, `--edu-success` ~1.72:1 non-text contrast) + A11Y-004 (minor, same token issue on group-panel banner dot) all fixed → `--edu-success-text` (5.24:1) + `conversationPresenceSuffix()` folding presence into the row's own aria-label. Design-review: PASS. Doc-sync follow-up flagged to uiux (not FE scope): design-spec.jsonc's literal `--edu-success`/`--edu-text-muted` values for this block should read the `-text`/`-secondary` accessible variants the implementation correctly uses. Mock-first (noti presence REST surface not confirmed live, OQ-2; NOTI_EP.presence path assumed). |
| US-E23.1 | Multi-Tenant Switch — header menu + dialog (DR-018 part 1): spec-only BA pass — no code yet | no | no | no | no | planned | docs/stories/epics/E23-multi-tenant-switch/US-E23.1-tenant-switch-menu/{requirements,integration,use-cases,story,spec}.md; high-risk lane (authorization hard gate); largely additive on existing SwitchTenantUseCase/switchTenantAction; tenant display fields (name/address/logoColor) mock-first, confirmed absent from iam wire schema |
| US-E23.2 | Multi-Tenant Switch — post-login select screen (DR-018 part 2): spec-only BA pass — no code yet | no | no | no | no | planned | docs/stories/epics/E23-multi-tenant-switch/US-E23.2-post-login-select-tenant/{requirements,integration,use-cases,story,spec}.md; high-risk lane; CONFIRMED as in-place enhancement of the existing `(auth)/select-tenant` screen owned by US-001-tenant-path-resolver (E05), not a new parallel screen |
| US-E03.1 | Principal Reports Dashboard: term-scoped stat cards + subject-average/attendance-trend charts (div-based, role=img) + periodic-reports table with polling generate→ready, principal-only route gate scoped to `principal/reports` only, mock-first `IPrincipalReportsRepository` (5 endpoints, INT-001..005); anti-demo mandate (NFR-005/AC-05.3) enforced via explicit one-shot `forceNextFailure` opt-in mock, no ordinal/counter/session forced-failure logic anywhere | yes | yes | yes | yes | implemented | 262 files / 1449 Vitest tests pass (incl. anti-demo fresh-3x-never-rejects + forceNextFailure negative proof, poll-transition via injected clock, no-ghost-row); 5 Storybook files / 20 stories pass; `bunx tsc --noEmit` clean; `bun lint` clean; `bun run build` green (`/principal/reports` route emitted); `fe-tech-lead-reviewer` Approved; `fe-accessibility-auditor` A11Y-001..007 (1 blocker+5 major+1 minor) all fixed + re-verified; design-review gate pass (docs/DESIGN_REVIEW.md); `fe-qa-playwright` Go (route-gate + AC-01.4 regression tests added, viewport 320/768/1280 real-computed-style assertions added); FR-009 Export Excel shipped as client-side CSV (not binary .xlsx, no new dependency/ADR needed) |
| US-E11.7 | Student Assignments (list, submit, graded feedback), `(app)/student/assignments` | yes | yes | yes | no | implemented | docs/stories/epics/E11-lms-exams/US-E11.7-student-assignments/{requirements,integration,use-cases,story,spec,plan}.md; mock-first `lms` service (decision 0014, no real assignment endpoint/openapi.yaml exists yet); 62/62 AC coverage, 57/62 passing on first QA pass, all 5 real defects (sheet-not-closing on stale submit, dialog focus-restore, card title-wrap) fixed and re-verified — 62/62 AC passing; unit+integration 1588/1588 (`bun vitest run`), Storybook interaction 42/42 for this slice; `tsc`/`build` clean; design-review gate: pass (tokens/a11y/states); platform (manual keyboard-only pass) not yet performed — flagged as remaining follow-up |

## Evidence Rules

- Unit proof covers pure domain and application rules.
- Integration proof covers backend enforcement, data integrity, provider
  behavior, jobs, or service contracts.
- E2E proof covers user-visible browser flows.
- Platform proof covers only shell, deployment, mobile, desktop, or runtime
  behavior that cannot be proven in lower layers.
- A story can be implemented without every proof column if the story packet
  explains why.
